Question 1. What is Flutter?
A. A new programming language developed by Google
B. A UI toolkit by Google for building natively compiled apps from a single codebase
C. A framework only for building web applications ⏎
D. A mobile operating system by Google
Answer: B. Flutter is Google's UI toolkit for building natively compiled apps from one codebase. (A is wrong because Flutter is not a language; C is wrong because Flutter is cross-platform, not just web; D is wrong because Flutter is not an OS.)


Question 2. Which programming language and rendering engine does Flutter use?
A. Kotlin and Skia
B. Dart and Skia
C. Dart and V8
D. Swift and Metal
Answer: B. Flutter uses Dart with the Skia rendering engine. (A, C, D all pair the wrong language/engine.)


Question 3. Flutter's architecture has three main layers. What is the correct top-to-bottom order (closest to the developer to closest to the hardware)? ⏎ A. Engine → Framework → Embedder
⏎ B. Framework → Engine → Embedder
⏎ C. Embedder → Framework → Engine
⏎ D. Framework → Embedder → Engine
Answer: B. Architecture order: Framework (Dart) → Engine (C++) → Embedder — the standard order stated in the 'Flutter Architecture Overview' slide. Other options reverse the order incorrectly.


Question 4. Which layer of the Flutter architecture is written in C++ and uses Skia to handle rendering, accessibility, and text layout?
A. Framework Layer
B. Engine Layer
C. Embedder Layer
D. Widget Layer
Answer: B. The Engine Layer is written in C++ and uses Skia for rendering/accessibility/text layout. Framework Layer (A) is the Dart layer above it; Embedder (C) handles platform integration; 'Widget Layer' (D) is not an official name for this architecture layer.


Question 5. What is the main role of the Embedder Layer in Flutter's architecture?
A. Provide all Material and Cupertino widgets
B. Compile Dart code to ARM machine code
C. Platform-specific integration, handling input, lifecycle, and window management
D. Perform JSON parsing from APIs
Answer: C. The Embedder Layer handles platform-specific integration, input, lifecycle, and window management. A and D belong to the Framework/networking layers; B is the job of the Dart AOT compiler, not the Embedder.


Question 6. A startup wants to build an MVP quickly, running on both Android and iOS, with a limited development team and no need for extreme graphics performance. According to the lecture, this situation is: ⏎ A. Not suitable for Flutter because Flutter only supports Android ⏎ B. A reasonable fit for Flutter because it matches the goal of faster time-to-market and reduced development cost ⏎ C. Better suited to pure native code because Flutter doesn't support MVPs ⏎ D. Best done in Kotlin because Flutter is not optimized for commercial apps
Answer: B. The lecture states Flutter suits MVPs, startups, cost reduction, and time-to-market — matching this scenario exactly. A, C, D all contradict the lecture content.


Question 7. Which of the following is the best example of a case where you should NOT choose Flutter and should build natively instead? ⏎ A. A simple movie ticket booking app ⏎ B. An e-commerce app with a standard UI ⏎ C. A real-time graphics-intensive game requiring low-level hardware access ⏎ D. An internal enterprise management dashboard app
Answer: C. A real-time, graphics-intensive game requiring low-level hardware access is the textbook example of 'When Native Development Is Preferable.' A, B, D are all cases where Flutter fits well.


Question 8. Flutter communicates with native features (Camera, Bluetooth, NFC...) that it does not support directly through which mechanism? ⏎ A. WebSocket ⏎ B. Platform Channels ⏎ C. REST API ⏎ D. Automatic FFI Bridge
Answer: B. Flutter integrates with native features via Platform Channels. A, C, D are not the mechanism covered in the lecture.


Question 9. Which combined compilation model does Dart use to support both fast development and optimized production performance? ⏎ A. JIT (Just-In-Time) only ⏎ B. AOT (Ahead-Of-Time) only ⏎ C. Both JIT (development) and AOT (production) ⏎ D. Purely interpreted, no compilation
Answer: C. Dart combines JIT (fast development, hot reload) and AOT (high-performance production compilation). A and B state only half the truth; D is entirely wrong.


Question 10. In the framework comparison table from Module 1, what is the core difference between Flutter and React Native? ⏎ A. Flutter uses Dart and compiles to native UI with its own engine, while React Native uses JavaScript and bridges to native components ⏎ B. Both compile directly to native machine code without an engine ⏎ C. React Native is faster because it needs no bridge ⏎ D. Flutter cannot build for iOS, only Android
Answer: A. Comparison table: Flutter (Dart, Native UI + Engine) vs React Native (JS, Bridge to Native) vs Native (Kotlin/Swift, Pure Native) — as shown in the slide. B is wrong because React Native needs a bridge; C and D are unsupported claims.


Question 11. In a Flutter project, which file is the entry point of the app? ⏎ A. pubspec.yaml ⏎ B. lib/main.dart ⏎ C. android/build.gradle ⏎ D. web/index.html
Answer: B. `lib/main.dart` is the entry point, as stated in the project structure table. A is the dependency config file; C, D are platform config files, not the entry point.


Question 12. Which folder in the Flutter project structure holds dependency and asset configuration? ⏎ A. lib/ ⏎ B. test/ ⏎ C. pubspec.yaml ⏎ D. android/
Answer: C. `pubspec.yaml` holds dependencies and assets configuration. A holds the main Dart code; B holds unit tests; D holds Android-specific code.


Question 13. What is the `flutter doctor` command used for? ⏎ A. Automatically fix errors in Dart source code ⏎ B. Check and confirm whether the Flutter SDK installation and related tools are ready ⏎ C. Build the app into a release APK file ⏎ D. Run unit tests for the whole project
Answer: B. `flutter doctor` checks the Flutter SDK installation environment and related tools. A, C, D misdescribe this command's function.


Question 14. A student creates an emulator on a low-spec PC. According to the lecture's recommendation, which emulator configuration is more suitable to reduce the load on the machine? ⏎ A. Pixel 8, API 33+ ⏎ B. Pixel 3a, API 30 ⏎ C. Pixel Tablet, API 34 ⏎ D. Pixel Fold, API 35
Answer: B. The lecture recommends Pixel 3a / API 30 for low-spec machines. A, C, D are heavier configurations, unsuitable for weak machines.


Question 15. How does DartPad differ from VS Code/Android Studio when developing Flutter? ⏎ A. DartPad requires a full SDK installation just like Android Studio ⏎ B. DartPad is an online environment, requires no installation, and runs directly in the browser ⏎ C. DartPad does not support basic widgets like Text, Column ⏎ D. DartPad can only be used for the Kotlin language
Answer: B. DartPad is an online environment requiring no installation. A is wrong because DartPad needs no full SDK; C is wrong because DartPad supports Text, Column; D is wrong because this is Dart/Flutter, not Kotlin.


Question 17. According to Flutter's architecture, which statement best describes the concept of a 'Widget'? ⏎ A. Widgets are only components that display images ⏎ B. In Flutter, everything on screen (layout, style, interaction) is built through Widgets ⏎ C. Widgets only exist in the Engine Layer ⏎ D. Widget is an Android-only concept, not used in Flutter
Answer: B. 'Everything is a Widget' — everything on screen is a widget. A wrongly limits the scope; C, D are wrong about where/what a widget is.


Question 18. According to the development history timeline mentioned in the lecture, what platform support did Flutter 2.0 (2021) add? ⏎ A. Only added Android Wear support ⏎ B. Web and Desktop ⏎ C. Only added iOS support ⏎ D. Only added watchOS support
Answer: B. Flutter 2.0 (2021) added Web and Desktop support per the lecture's timeline. A, C, D do not match the stated milestones.


Question 19. What order does Flutter's rendering workflow follow? ⏎ A. GPU Rendering → Widget Tree → Render Tree → Composited Scene ⏎ B. Widget Tree → Render Tree → Composited Scene → GPU Rendering ⏎ C. Render Tree → Widget Tree → GPU Rendering → Composited Scene ⏎ D. Composited Scene → Widget Tree → GPU Rendering → Render Tree
Answer: B. Render workflow: Widget Tree → Render Tree → Composited Scene → GPU Rendering, matching the slide's order exactly. Other options reverse the order incorrectly.


Question 20. An instructor creates a new Flutter project with Organization ID `com.fptu.movies` and project name `movies`. What is the main purpose of the Organization ID during project creation? ⏎ A. Only used to set the display name shown on the device's home screen ⏎ B. Determines the unique package name/bundle identifier for the app when packaged for Android/iOS ⏎ C. Has no real effect, purely illustrative ⏎ D. Used to set the app's color theme
Answer: B. Organization ID determines the unique package name/bundle identifier when packaging the app. A, C, D misdescribe its technical role.


Question 21. According to the lecture, when should a development team consider using native code (Kotlin/Swift) instead of pure Flutter? ⏎ A. When the app needs very low-level hardware access or strict platform-specific UI/UX requirements ⏎ B. When the app only needs to run on a simple single Android platform ⏎ C. When the team has little experience with Kotlin/Swift ⏎ D. When the app only displays a static list of text
Answer: A. Native should be considered for very low-level hardware access or strict platform-specific UI/UX — matching 'When Native Development Is Preferable.' B, C, D are cases where Flutter still fits or are unrelated.


Question 22. In the 'Hello World' example run across three environments (DartPad, VS Code, Android Studio), what is the most important common point emphasized? ⏎ A. Each environment uses a different programming language ⏎ B. All tools compile down to the same Flutter runtime, producing identical results ⏎ C. Only Android Studio can run a real app ⏎ D. DartPad cannot display TextStyle
Answer: B. The key common point: all tools compile to the same Flutter runtime, giving identical results across DartPad/VS Code/Android Studio. A, C, D are all incorrect per the lecture.


Question 23. Given the comparison table: Flutter (Dart, Native UI + Engine), React Native (JS, Bridge to Native), Native (Kotlin/Swift, Pure Native). Which statement best analyzes the trade-off among these three approaches? ⏎ A. Native is always slower than Flutter because it has no dedicated engine ⏎ B. React Native is fastest because it needs no compilation ⏎ C. Flutter achieves near-native performance thanks to its own renderer engine (Skia) without needing a bridge like React Native, while Native achieves maximum performance but loses the shared-codebase advantage ⏎ D. All three produce identical pure native code
Answer: C. Flutter achieves near-native performance via its own Skia engine without a bridge like React Native; Native achieves top performance but loses the shared codebase — this is the accurate trade-off per the comparison table. A, B, D are wrong or oversimplified.


Question 24. According to the lecture, what tool does macOS need to build a Flutter app for iOS? ⏎ A. Android Studio ⏎ B. Xcode ⏎ C. Visual Studio ⏎ D. IntelliJ IDEA
Answer: B. macOS needs Xcode to build for iOS, per the note 'macOS users need Xcode for iOS builds.' A, C, D are not the tool stated for this purpose.


Question 25. Which statement is FALSE regarding the reasons to choose Flutter ('Why Flutter')? ⏎ A. Hot Reload/Hot Restart enables fast development ⏎ B. Provides Material & Cupertino widgets for a beautiful UI ⏎ C. Can only run on a single platform ⏎ D. Near-native performance
Answer: C. 'Can only run on a single platform' is FALSE because Flutter is cross-platform (Android, iOS, Web, Desktop) — this is Flutter's core strength, the exact opposite of what 'Why Flutter' states. A, B, D are all valid reasons stated in the lecture.


Question 26. In Dart, what is the basic difference between `final` and `const`? ⏎ A. No difference, the two keywords are interchangeable in every case ⏎ B. `final` is a compile-time constant, `const` is a runtime constant ⏎ C. `final` is a value assigned once at runtime (runtime-constant), `const` is a compile-time constant ⏎ D. `final` is only for numeric types, `const` is only for string types
Answer: C. `final` is assigned once at runtime; `const` is a value known ahead of time at compile-time. B reverses the definitions; D is wrong since neither is limited to a specific data type.


Question 31. What does the `??` operator do in Dart? ⏎ A. Type equality comparison ⏎ B. Returns the left-hand value if it's not null, otherwise returns the right-hand value ⏎ C. Forces a non-null cast, throwing an error if null ⏎ D. Logical XOR operator
Answer: B. `??` returns the left side if non-null, otherwise the right side (null-coalescing). A, C, D misdescribe this operator's function.


Question 34. What is the difference between `for-in` and `forEach` when iterating a List in Dart? ⏎ A. `for-in` cannot be used with a List ⏎ B. `forEach` is the classic index-based loop syntax, while `for-in` is a higher-order function ⏎ C. `for-in` is syntax for iterating each element of a collection, while `forEach` calls a callback function in a functional programming style for each element ⏎ D. The two syntaxes are entirely identical in performance and syntax, with no difference
Answer: C. `for-in` iterates each collection element; `forEach` is a higher-order function taking a callback in a functional style. A is wrong since for-in works with List; B reverses the description; D is wrong since there is a stylistic difference.


Question 40. In Dart's try/catch, what does the `catch (e, s)` syntax allow a developer to do? ⏎ A. Catch two different exception types at once ⏎ B. Catch the error object and its corresponding stack trace ⏎ C. Catch errors on two different threads ⏎ D. Only applicable to String-type errors
Answer: B. `catch (e, s)` catches both the error object and the stack trace. A, C, D misdescribe this syntax's function.


Question 41. Which statement is correct about the differences between `List`, `Set`, and `Map` in Dart? ⏎ A. List is unordered and disallows duplicates, Set is ordered, Map stores key-value pairs ⏎ B. List is an ordered collection, Set is a collection of unique (unordered) elements, Map stores key-value pairs ⏎ C. All three are the same data type, just different names ⏎ D. Set is ordered and allows duplicates, List does not
Answer: B. List is ordered; Set is a collection of unique, unordered elements; Map stores key-value pairs — the standard definition. A, C, D all misdescribe the basic characteristics.


Question 43. In Dart, what is the `Never` type used for? ⏎ A. A variable that is never assigned a value ⏎ B. A function that never returns normally (always throws an exception or loops infinitely) ⏎ C. A data type equivalent to `void` ⏎ D. A type used for asynchronous functions
Answer: B. `Never` is used for functions that never return normally (always throw or loop infinitely). A, C, D misdescribe the purpose of the Never type.


Question 44. In Dart, what is the role of the `main()` function? ⏎ A. It's a callback called last when the app closes ⏎ B. It's the entry point of execution for every Dart program ⏎ C. Only used in Flutter, not in pure Dart ⏎ D. A function used to define classes
Answer: B. `main()` is the entry point of execution for every Dart program. A, C, D misdescribe the role of the main function.


Question 47. In Dart's switch-case, why is `break` (or `return`) usually needed after each case? ⏎ A. Because Dart auto falls-through like C if there's no break, which can accidentally execute the next case ⏎ B. Because Dart requires this syntax to compile, unrelated to behavior ⏎ C. Because break is used to exit the main function ⏎ D. Dart doesn't support switch-case
Answer: A. Dart falls through like C if break is missing, which can accidentally execute the following case. B, C, D misdescribe break's role in switch-case.


Question 49. According to the lecture, what common pitfall is emphasized for students new to Dart? ⏎ A. Forgetting to close a class's curly braces ⏎ B. Ignoring null-safety warnings and improperly mixing synchronous/asynchronous code ⏎ C. Using too many async functions causing incorrect parallel execution ⏎ D. Being unable to create a String-type variable
Answer: B. The stated common pitfalls: ignoring null-safety warnings and improperly mixing sync/async code. A, C, D are not content emphasized in the lecture.


Question 51. What characterizes an abstract class in Dart? ⏎ A. It cannot have any methods with a body ⏎ B. It defines structure without full behavior, forcing subclasses to implement the bodiless methods ⏎ C. Only usable inside the Flutter Framework, not in pure Dart ⏎ D. Can be instantiated directly with the `new` keyword
Answer: B. An abstract class defines structure without full behavior, forcing subclasses to implement it. A is wrong since abstract classes can have methods with a body; C is wrong since it works in pure Dart too; D is wrong since abstract classes cannot be instantiated directly.


Question 52. In Flutter, `State<T>` is a textbook example of which concept from Module 3? ⏎ A. Mixin ⏎ B. Abstract class — forcing StatefulWidgets to override methods to define behavior ⏎ C. Generic Constraint ⏎ D. Factory Constructor
Answer: B. `State<T>` is an abstract class, forcing StatefulWidgets to override methods to define behavior. A, C, D do not correctly describe State<T>'s nature.


Question 53. Given the comparison table of `extends`, `implements`, `with`, which best describes `implements`? ⏎ A. Inherits all available behavior from the parent class ⏎ B. Only inherits the "signature" of the class/interface, forcing you to rewrite all custom behavior — used to define a contract ⏎ C. Used to share reusable code blocks without an inheritance relationship ⏎ D. Not supported by Dart, only exists in Java
Answer: B. `implements` only takes the signature (interface), forcing you to write all the behavior yourself, used as a contract. A describes `extends`; C describes `mixin`; D is wrong since Dart fully supports implements.


Question 55. What is a mixin used for in Dart? ⏎ A. Fully replaces constructors ⏎ B. Sharing behavior across multiple classes without a traditional inheritance chain ⏎ C. Used exclusively for JSON handling ⏎ D. A special kind of variable in Dart
Answer: B. A mixin shares behavior across multiple classes without a traditional inheritance chain. A, C, D misdescribe the purpose of a mixin.


Question 57. How does a factory constructor differ from a regular constructor in Dart? ⏎ A. A factory constructor always creates a completely new object, with no exceptions ⏎ B. A factory constructor allows control over instance creation — it can return an existing instance (cache/singleton) or parse data (JSON) instead of always returning a new object ⏎ C. A factory constructor cannot be named ⏎ D. A factory constructor can only be used with an abstract class
Answer: B. A factory constructor controls instance creation — it can return an existing instance or parse data, instead of always creating new. A is wrong since this is the exact difference; C, D are incorrect about syntax/scope.


Question 59. What is the main benefit that generics (`List<T>`, `Future<T>`, `Map<K,V>`) bring in Dart? ⏎ A. Makes the program run faster on the CPU ⏎ B. Ensures compile-time type safety, avoiding errors from misusing the dynamic type ⏎ C. Allows completely skipping data type declarations ⏎ D. Only works with primitive types (int, double)
Answer: B. Generics ensure compile-time type safety, avoiding dynamic-type misuse errors. A, C, D misdescribe the core benefit of generics.


Question 62. What does collection-if / collection-for syntax allow in Dart? ⏎ A. Only usable in a regular for loop, not in a List declaration ⏎ B. Flexibly building a List/Set/Map by inserting conditional elements (`if`) or generating elements from a loop (`for`) directly inside the collection declaration ⏎ C. Fully replaces the `var` keyword ⏎ D. Only applies to Map, not to List
Answer: B. Collection-if/for lets you flexibly build collections with conditional insertion or loop-generated elements right inside the declaration. A, C, D misdescribe the scope of application.


Question 64. What benefit does a Custom Exception in Dart (e.g. `class LoginException implements Exception`) offer over throwing an ordinary error string? ⏎ A. No benefit, it just adds code complexity ⏎ B. Provides specific business-domain error context, making it easier to debug and handle logic at the UI/Repository layer ⏎ C. Custom Exceptions run faster than plain Strings ⏎ D. Custom Exceptions can only be used with external APIs, not internally
Answer: B. Custom Exceptions provide specific domain error context, aiding debugging and handling at the UI/Repository layer. A, C, D don't match the real benefit stated.


Question 65. In Dart, which queue has HIGHER priority between the Microtask queue and the Event queue? ⏎ A. The Event queue has higher priority, always running before the Microtask queue ⏎ B. The Microtask queue has higher priority — all microtasks run before Dart continues processing the Event queue (Futures, timers, UI events) ⏎ C. Both run in parallel, with no priority order ⏎ D. Priority depends on the declaration order in the code, not fixed
Answer: B. The Microtask queue has higher priority — all microtasks run before Dart proceeds to the Event queue. A reverses this; C, D don't match Dart's event loop mechanism.


Question 67. Comparing Future chaining (`.then()`) with `async/await`, which statement is most accurate? ⏎ A. `.then()` and `async/await` are entirely incompatible, cannot be used together in a project ⏎ B. `.then()` suits simple async chains without needing intermediate variables; `async/await` makes async code read linearly, easier to follow when there are multiple dependent steps ⏎ C. `async/await` runs fully synchronously, no longer asynchronous ⏎ D. `.then()` is always faster than `async/await` in terms of performance
Answer: B. `.then()` suits simple chains; `async/await` helps code read linearly when steps depend on each other. A is wrong since they can be used together; C is wrong since async/await is still asynchronous; D has no basis regarding absolute performance.


Question 68. How does `StreamController.broadcast()` differ from the default single-subscription Stream? ⏎ A. Broadcast only allows exactly one listener ⏎ B. Broadcast allows multiple listeners to listen simultaneously and receive shared events, while single-subscription only allows one listener, consuming sequentially ⏎ C. No real difference, just a different name ⏎ D. Broadcast only works with String data types
Answer: B. Broadcast allows multiple listeners to receive shared events; single-subscription allows only one. A reverses this; C, D don't match the actual characteristics of the two Stream types.


Question 70. What is the main purpose behind introducing the Repository Pattern in Module 3? ⏎ A. Fully replaces Widgets in Flutter ⏎ B. Separates data-fetching logic from the presentation/UI layer, making code easier to test and maintain, and forming the foundation for Bloc/Provider/Riverpod ⏎ C. Only used for apps with no UI ⏎ D. A replacement for SQLite
Answer: B. The Repository Pattern separates data fetching from UI, aiding testability/maintainability, and forms the foundation for Bloc/Provider/Riverpod. A, C, D misdescribe its purpose.


Question 72. According to the "Performance Best Practices" section of Module 3, which practice is NOT recommended? ⏎ A. Using const widgets to reduce rebuild cost ⏎ B. Running a heavy loop directly on the main isolate while building the UI ⏎ C. Using async functions for I/O operations ⏎ D. Using Isolates for heavy CPU tasks
Answer: B. Running a heavy loop directly on the main isolate while building UI is the practice that should NOT be done (opposite of best practice). A, C, D are all recommended practices.


Question 73. In Dart, what is the core difference between an `abstract class` and an `implicit interface` (every class automatically has an interface)? ⏎ A. No difference, the two concepts are the same ⏎ B. An abstract class can contain both bodied and bodiless methods and cannot be instantiated directly; while any regular class automatically defines an implicit interface that other classes can `implements` ⏎ C. Interfaces only exist in Java, Dart has no such concept ⏎ D. Abstract classes are always faster than interfaces at runtime
Answer: B. Abstract class can mix bodied/bodiless methods and can't be instantiated directly; any regular class automatically has an implicit interface others can implement. A is wrong since there's a clear difference; C is wrong since Dart has this concept via implicit interfaces; D has no basis regarding absolute runtime performance.


Question 74. In the example `AnimationController implements Listenable` mentioned in the lecture, what does using `implements` instead of `extends` demonstrate? ⏎ A. AnimationController copies all of Listenable's existing code ⏎ B. AnimationController only follows Listenable's contract and implements its own behavior, without inheriting existing implementation ⏎ C. AnimationController and Listenable are unrelated to each other ⏎ D. This is a syntax error, `extends` must be used
Answer: B. `implements` means AnimationController only follows the contract and implements its own behavior, without inheriting existing implementation from Listenable. A is wrong since implements doesn't copy code; C is wrong since there's a clear relationship via implements; D is wrong since this is valid Dart syntax.


Question 76. In Flutter, which widget acts as the "backbone" of a Material screen, providing AppBar, body, and FloatingActionButton? ⏎ A. Container ⏎ B. Scaffold ⏎ C. MaterialApp ⏎ D. Column
Answer: B. Scaffold is the backbone of a Material screen, providing AppBar/body/FAB. A, C, D don't correctly describe this structural role.


Question 77. A student places a `ListView` directly inside a `Column` without wrapping it in `Expanded` or `Flexible`. What common error occurs? ⏎ A. The app runs completely normally, no issues ⏎ B. A "RenderFlex overflow" error or unbounded height constraint error, because both Column and ListView try to claim unlimited space along the main axis ⏎ C. The ListView automatically shrinks to exactly 1 item ⏎ D. The Column is completely hidden from the screen
Answer: B. Placing a ListView in a Column without Expanded/Flexible causes an unbounded height error (overflow). A is wrong since this is exactly the common bug; C, D don't match Flutter's actual behavior.


Question 78. In RadioListTile, what is the role of the `groupValue` property? ⏎ A. Determines that particular radio button's own value ⏎ B. Holds the currently selected value for the entire group of RadioListTiles, used to compare against each item's `value` to determine selected state ⏎ C. Has no effect, purely decorative ⏎ D. Used to set the background color of RadioListTile
Answer: B. `groupValue` holds the currently selected value for the whole group, compared with each item's `value`. A, C, D misdescribe its function.


Question 80. What is the difference between `Slider` and `Switch` in Flutter? ⏎ A. Slider selects a continuous value within a range, Switch only toggles a boolean value ⏎ B. Both are used for boolean values ⏎ C. Switch is for real numbers, Slider is for boolean ⏎ D. No functional difference
Answer: A. Slider selects a continuous value, Switch only toggles boolean. B, C, D misdescribe/reverse the two widgets' functions.


Question 81. What does `ThemeMode.system` mean in `MaterialApp`? ⏎ A. Forces the app to always use the light theme ⏎ B. Forces the app to always use the dark theme ⏎ C. The app automatically follows the device's OS-level light/dark appearance setting ⏎ D. Has no effect if darkTheme isn't declared
Answer: C. `ThemeMode.system` makes the app follow the device OS's light/dark setting automatically. A, B fix one mode, contradicting "system"; D is wrong since themeMode still has meaning but full effect needs darkTheme declared.


Question 83. How does `ListView.separated` differ from `ListView.builder`? ⏎ A. `ListView.separated` doesn't support itemBuilder ⏎ B. `ListView.separated` allows inserting a separator widget between items via `separatorBuilder`, which `ListView.builder` lacks ⏎ C. `ListView.builder` only displays up to 10 items ⏎ D. The two are entirely identical in API
Answer: B. `ListView.separated` allows inserting a separator between items via `separatorBuilder`. A is wrong since it still has itemBuilder; C is wrong since there's no 10-item limit; D is wrong since the two APIs differ.


Question 85. In the `showDatePicker` example, what are the `firstDate` and `lastDate` parameters used for? ⏎ A. Determine the default current date ⏎ B. Restrict the valid date range the user can select on the calendar ⏎ C. No real effect, purely a required syntax parameter ⏎ D. Determine the date display format (dd/mm/yyyy or mm/dd/yyyy)
Answer: B. `firstDate`/`lastDate` restrict the valid selectable date range on the calendar. A, C, D misdescribe these two parameters' function.


Question 87. How does the `Wrap` widget differ from `Row`, and why does it suit a genre chip list? ⏎ A. Wrap is identical to Row, no difference ⏎ B. Wrap automatically wraps child elements to a new line when there isn't enough horizontal space, avoiding the overflow error Row would encounter with too many elements ⏎ C. Wrap only allows a maximum of 2 child elements ⏎ D. Row auto-wraps but Wrap doesn't
Answer: B. Wrap automatically wraps children to a new line when out of horizontal space, avoiding the overflow Row would suffer. A, C, D misdescribe/reverse the two widgets' characteristics.


Question 89. According to general recommendations, `BottomNavigationBar` best fits apps with how many main navigation tabs? ⏎ A. 1 tab ⏎ B. 2-5 tabs ⏎ C. 10-15 tabs ⏎ D. No limit on the number
Answer: B. BottomNavigationBar best suits 2-5 tabs per general recommendation. A, C, D don't match the recommended number.


Question 91. What is the difference between `Image.network` and `Image.asset`? ⏎ A. `Image.network` loads an image from an internet URL, `Image.asset` loads an image bundled within the project (declared in pubspec.yaml) ⏎ B. Both only load images from local storage ⏎ C. `Image.asset` only works on Web ⏎ D. `Image.network` doesn't support BoxFit
Answer: A. `Image.network` loads from a URL, `Image.asset` loads bundled project images. B, C, D misdescribe the two image widgets' functions.


Question 92. In Module 4's "Common Errors & Fixes" list, what is the common cause of the UI "not updating" after data changes? ⏎ A. Network error ⏎ B. Forgetting to call `setState()` after changing a variable's value in a StatefulWidget ⏎ C. Missing Scaffold ⏎ D. Incorrect import syntax
Answer: B. Forgetting to call `setState()` is the common cause of a UI not updating, per the "Common Errors & Fixes" checklist. A, C, D are not the cause stated in the lecture.


Question 94. According to the lecture, what are Material Icons in Flutter commonly used for? ⏎ A. Purely decorative, no function ⏎ B. Navigation buttons, action buttons (add, delete), and status indicators ⏎ C. Only used as a Scaffold background ⏎ D. Completely replaces the Text widget
Answer: B. Material Icons are used for navigation buttons, action buttons, and status indicators. A, C, D misdescribe their purpose.


Question 95. Comparing `TimePicker` (`showTimePicker`) and `DatePicker` (`showDatePicker`) in the context of building a booking flow combining both date and time: what is the correct approach per the lecture? ⏎ A. Only call one of the two, Flutter will automatically infer the other value ⏎ B. Call two separate async functions sequentially (`pickDate()` and `pickTime()`), store the results in two separate State variables (`DateTime? d`, `TimeOfDay? t`), then display them combined in the UI ⏎ C. Use a single built-in `DateTimePicker` widget in the Flutter SDK ⏎ D. It's impossible to combine DatePicker and TimePicker on the same screen
Answer: B. Call `pickDate()` and `pickTime()` sequentially, store results in two separate State variables, then display combined — matching the Booking demo's approach. A, C, D don't reflect the taught implementation.


Question 96. In VS Code, what does the "Wrap with Widget" feature support for a Flutter developer? ⏎ A. Automatically deploy the app to a store ⏎ B. Quickly wrap a selected widget with another widget like Padding, Center, Expanded to refactor layout ⏎ C. Automatically fix all syntax errors in the file ⏎ D. Convert Dart to Kotlin
Answer: B. "Wrap with Widget" quickly wraps a selected widget with Padding/Center/Expanded to refactor layout. A, C, D don't match this feature's function.


Question 97. In a basic Scaffold example, where is `floatingActionButton` typically placed and what is it used for? ⏎ A. In the middle of the screen, as a background for the whole body ⏎ B. Usually floats in the bottom-right corner of the screen, used for a prominent primary action (e.g. add new) ⏎ C. Only displayed inside the AppBar ⏎ D. No fixed position, always centered in the AppBar
Answer: B. floatingActionButton typically floats bottom-right, used for a prominent primary action. A, C, D misdescribe its position/role.


Question 99. According to Module 4, when should `SingleChildScrollView` or `Flexible` be used to fix overflow on small screens? ⏎ A. Never needed, Flutter automatically handles overflow ⏎ B. When UI content exceeds the available screen size (especially on small screens), you need scrolling or flexible resizing to avoid layout overflow ⏎ C. Only used for apps running on Desktop ⏎ D. Only applicable to the Text widget
Answer: B. Use them when content exceeds the available screen size, to allow scrolling or flexible resizing and avoid overflow. A is wrong since Flutter doesn't auto-handle every overflow case; C, D wrongly restrict the scope of use.


Question 100. Consider Module 4's "Practice Task": build a movie list using `ListView.builder` + `ListTile`, where `onTap` needs to show `SnackBar(title)`. What is the correct way to call a SnackBar in Flutter? ⏎ A. `Text(title).showSnackBar()` ⏎ B. `ScaffoldMessenger.of(context).showSnackBar(SnackBar(content: Text(title)))` ⏎ C. `Navigator.of(context).showSnackBar(title)` ⏎ D. `SnackBar.show(context, title)`
Answer: B. `ScaffoldMessenger.of(context).showSnackBar(SnackBar(content: Text(title)))` is the correct call. A, C, D are not valid Flutter APIs.


Question 101. In Flutter, what does `Navigator.push()` do? ⏎ A. Clears the entire navigation stack ⏎ B. Adds (pushes) a new route/screen onto the top of the navigation stack ⏎ C. Goes back to the previous screen ⏎ D. Closes the app
Answer: B. `Navigator.push()` pushes a new route onto the top of the navigation stack. A, C, D misdescribe this method's function.


Question 102. What does the `Navigator.pop(context)` method do? ⏎ A. Pushes another new screen onto the stack ⏎ B. Removes (pops) the screen currently at the top of the stack, revealing the screen below it ⏎ C. Replaces the entire stack with a new route ⏎ D. Restarts the app from scratch
Answer: B. `Navigator.pop(context)` removes the top screen from the stack, showing the one below. A, C, D misdescribe this behavior.


Question 104. What is the difference between `Navigator.push()` and `Navigator.pushReplacement()`? ⏎ A. No difference ⏎ B. `push()` adds a new route on top of the current one (the old route stays in the stack), while `pushReplacement()` replaces the current route with a new one (the old route is removed from the stack) ⏎ C. `pushReplacement()` can only be used with named routes ⏎ D. `push()` doesn't allow going back with the back button
Answer: B. `push()` keeps the old route in the stack; `pushReplacement()` removes the old route when adding the new one. A is wrong since there's a clear difference; C, D are not accurate technical characteristics.


Question 105. In a login flow, a developer uses `Navigator.pushReplacementNamed(context, '/home')` instead of `Navigator.pushNamed(context, '/home')` after a successful login. What is the most reasonable reason for this choice? ⏎ A. `pushReplacementNamed` runs faster in terms of performance ⏎ B. It prevents the user from pressing Back to return to the Login screen after successfully logging in, since the Login route has been removed from the stack ⏎ C. `pushNamed` doesn't support passing parameters ⏎ D. There's no real difference between the two calls
Answer: B. `pushReplacementNamed` prevents returning to Login via Back since the Login route is removed from the stack. A, C, D are not the correct reason.


Question 106. What benefit do Named Routes in Flutter bring over calling `MaterialPageRoute` directly everywhere? ⏎ A. Named Routes run faster at runtime ⏎ B. They increase readability and scalability in large apps, especially when parameters need to be passed or many screens need centralized management ⏎ C. Named Routes are the only way Flutter supports navigation ⏎ D. Named Routes only work with Navigator 2.0
Answer: B. Named Routes improve readability and scalability in large apps, especially for parameter passing and centralized screen management. A, C, D don't match the actual benefit.


Question 107. What is the core difference between Navigator 1.0 and Navigator 2.0? ⏎ A. Navigator 1.0 uses a declarative model, Navigator 2.0 uses an imperative model ⏎ B. Navigator 1.0 is imperative — manually calling push()/pop(); Navigator 2.0 is declarative — describing the desired stack state and letting the framework sync automatically ⏎ C. Both are entirely identical, just different names across Flutter versions ⏎ D. Navigator 2.0 doesn't support deep linking, only Navigator 1.0 does
Answer: B. Navigator 1.0 is imperative (manual push/pop); Navigator 2.0 is declarative (describes the desired stack, framework syncs). A reverses this; C, D don't match the two versions' characteristics.


Question 108. What are the three main components in Navigator 2.0's architecture, as mentioned in the lecture? ⏎ A. Router, Controller, View ⏎ B. RouterDelegate, RouteInformationParser, BackButtonDispatcher ⏎ C. Navigator, Route, Stack ⏎ D. Provider, Consumer, ChangeNotifier
Answer: B. The three main components: RouterDelegate, RouteInformationParser, BackButtonDispatcher — matching the slide. A, C, D are not the correct names of these components.


Question 109. What does deep linking in Flutter allow? ⏎ A. Increases app build speed ⏎ B. Lets the user open a specific screen in the app directly via a URL or external source (e.g. a web link or notification) ⏎ C. Only applicable to apps running on Web ⏎ D. A feature that fully replaces SharedPreferences
Answer: B. Deep linking lets users open a specific screen directly via a URL/external source. A, C, D misdescribe deep linking's purpose.


Question 111. On iOS, to support a custom URL scheme (e.g. `movieapps://`), where does the configuration need to be added? ⏎ A. AndroidManifest.xml ⏎ B. build.gradle ⏎ C. Info.plist (add URL Types → URL Schemes) ⏎ D. pubspec.yaml
Answer: C. Custom URL scheme config on iOS goes in Info.plist (URL Types → URL Schemes). A, B, D are not the correct location for this configuration.


Question 112. What is the basic difference between `StatelessWidget` and `StatefulWidget`? ⏎ A. StatelessWidget can change its UI more flexibly than StatefulWidget ⏎ B. StatelessWidget holds no mutable internal state and doesn't rebuild when internal data changes; StatefulWidget holds state (a State object) and can rebuild via `setState()` ⏎ C. Only StatelessWidget has a `build()` method ⏎ D. StatefulWidget is not allowed to contain child widgets
Answer: B. StatelessWidget has no mutable state and doesn't rebuild on its own; StatefulWidget holds a State object and rebuilds via setState(). A, C, D misdescribe the basic characteristics of the two widget types.


Question 114. What does "Lifting State Up" mean in Flutter? ⏎ A. Removing all state from the app ⏎ B. Moving shared state up to a common ancestor widget so child widgets can access and sync data, reducing duplicated logic ⏎ C. Moving all UI logic to the server side ⏎ D. Only applicable when using Provider
Answer: B. Lifting State Up moves shared state to a common ancestor to reduce duplicated logic and improve data flow. A, C, D misdescribe this concept.


Question 116. What foundation does `Provider` (ChangeNotifierProvider + Consumer) build on in Flutter, and what benefit does it bring over plain InheritedWidget? ⏎ A. Built on SQLite, helps store offline data ⏎ B. Built on top of InheritedWidget but reduces boilerplate code, making state sharing and listening much easier ⏎ C. Unrelated to InheritedWidget at all ⏎ D. Provider only works with the String data type
Answer: B. Provider is built on InheritedWidget but reduces boilerplate for state sharing/listening. A, C, D don't match Provider's actual technical foundation.


Question 119. In the Stateless/Stateful comparison table, which usage example fits StatelessWidget? ⏎ A. An input form with validation ⏎ B. A counter that increases/decreases with user interaction ⏎ C. A static logo, unchanging text ⏎ D. A list where items can be added/removed
Answer: C. A static logo/unchanging text fits StatelessWidget since there's no rebuild needed. A, B, D all require StatefulWidget due to changing state.


Question 122. In the deep link handling flow via `onGenerateRoute`, what is the main purpose of this callback in `MaterialApp`? ⏎ A. Only handles network errors ⏎ B. Allows the app to parse the route name (and parameters) coming from an external link, then dynamically return the corresponding route/screen, commonly used to handle deep links ⏎ C. Fully replaces Scaffold ⏎ D. Only works when the app is offline
Answer: B. `onGenerateRoute` parses the route name/parameters from an external link and dynamically returns the corresponding screen, commonly for deep links. A, C, D misdescribe its purpose.


Question 123. What does the `Route` class in Flutter represent? ⏎ A. An API endpoint from a server ⏎ B. A screen/page in the app, usually implemented via `MaterialPageRoute` ⏎ C. A type of animation ⏎ D. A JSON data structure
Answer: B. `Route` represents a screen/page in the app, typically implemented via MaterialPageRoute. A, C, D misdescribe the Route concept.


Question 125. According to Module 5's Summary, which state management techniques are covered, in order from basic to more advanced? ⏎ A. Provider → InheritedWidget → setState ⏎ B. setState → InheritedWidget → Provider ⏎ C. Only setState is covered, no other technique is mentioned ⏎ D. BLoC → Riverpod → GetX
Answer: B. Per the Summary, the order from basic to advanced is: setState → InheritedWidget → Provider. A reverses the order; C is incomplete; D is not the focus of Module 5 (only briefly mentioned as prep in Module 3).


Question 126. What is the core difference between "Responsive UI" and "Adaptive UI" per the lecture? ⏎ A. Responsive UI changes based on device type, Adaptive UI changes based on available space ⏎ B. Responsive UI changes layout based on available space (e.g. Column → Row), while Adaptive UI changes based on device type (e.g. Bottom Nav → NavigationRail on tablet) ⏎ C. The two concepts are identical, no real difference ⏎ D. Adaptive UI only applies to Web apps
Answer: B. Responsive UI reacts to available space; Adaptive UI reacts to device type. A reverses the two definitions; C, D don't match the lecture content.


Question 127. What information does `MediaQuery.of(context).size.width` retrieve? ⏎ A. The width of the current widget itself ⏎ B. The full width of the current device's screen ⏎ C. The width of the virtual keyboard ⏎ D. The number of child widgets in the tree
Answer: B. `MediaQuery.of(context).size.width` gets the current device's full screen width. A, C, D misdescribe the scope of the returned value.


Question 129. What is the core difference between `LayoutBuilder` and `MediaQuery`? ⏎ A. LayoutBuilder provides information about the direct parent widget's constraints, while MediaQuery provides the entire device screen's info ⏎ B. Both are entirely identical in function ⏎ C. LayoutBuilder only works on Web ⏎ D. MediaQuery only provides the parent widget's constraints, not the full screen
Answer: A. LayoutBuilder gives the direct parent's constraints; MediaQuery gives full-screen device info. B, C, D reverse/misstate the characteristics of the two tools.


Question 131. What is `OrientationBuilder` used for? ⏎ A. Detecting device rotation (portrait/landscape) and changing the UI accordingly ⏎ B. Only used to change the app's background color ⏎ C. Checking the device's network connection ⏎ D. Fully replaces MediaQuery in every case
Answer: A. OrientationBuilder detects device rotation and lets the UI change accordingly. B, C, D misdescribe its function.


Question 132. In the "Responsive Widgets Overview" list, which widget helps avoid overflow by letting a child take up the remaining space in a Row/Column? ⏎ A. Wrap ⏎ B. Expanded ⏎ C. AspectRatio ⏎ D. FractionallySizedBox
Answer: B. Expanded lets a child take the remaining space in a Row/Column, avoiding overflow. A, C, D serve other purposes, not specifically this one.


Question 134. What problem does `SafeArea` in Flutter solve on modern devices? ⏎ A. Increases the app's rendering speed ⏎ B. Avoids UI content being obscured by unusable areas like notches, Dynamic Island, or curved screens ⏎ C. Encrypts user data ⏎ D. Automatically translates the UI language
Answer: B. SafeArea avoids UI content being obscured by notches/Dynamic Island/curved screens. A, C, D misdescribe SafeArea's purpose.


Question 135. A student forgets to wrap a search bar in `SafeArea` on a device with a notch. What is the most likely real-world consequence? ⏎ A. The app automatically crashes right at startup ⏎ B. Part of the search bar's top area may be obscured or clipped by the notch/status bar, harming the user experience ⏎ C. GridView automatically switches its layout to 2 columns ⏎ D. No effect at all, SafeArea is purely cosmetic and unnecessary
Answer: B. Missing SafeArea can cause the top of the search bar to be obscured/clipped by the notch/status bar, harming UX. A, C, D don't reflect the realistic consequence.


Question 136. According to "Using Breakpoints" in the lecture, what layout-splitting rule based on width is proposed? ⏎ A. < 400px: Small, 400-800px: Medium, > 800px: Large ⏎ B. < 100px: Small, 100-300px: Medium, > 300px: Large ⏎ C. Only 2 levels: Mobile and Desktop, no intermediate level ⏎ D. Breakpoint is always fixed at 1024px in every case
Answer: A. Proposed breakpoint rule: <400px Small, 400-800px Medium, >800px Large — matching the slide. B, C, D don't match the stated numbers.


Question 137. In the Genre Chips example (Wrap-based chip list), the lecture emphasizes: "This behavior is achieved automatically by using Wrap, without conditional logic." What does this mean technically? ⏎ A. You need many `if` statements to check screen size then decide chips per row ⏎ B. The `Wrap` widget automatically calculates and wraps elements to a new line based on available space, without the developer writing manual conditional logic for responsiveness ⏎ C. Wrap only works correctly when combined with LayoutBuilder ⏎ D. This description is wrong, Wrap always needs accompanying conditional logic
Answer: B. `Wrap` automatically calculates and wraps elements based on available space without manual conditional logic. A, C, D don't match the intended meaning of the quoted statement.


Question 139. What is the difference between `Flexible` and `Expanded` in a Row/Column? ⏎ A. No difference, the two widgets are entirely equivalent ⏎ B. `Expanded` forces a child to occupy the entire allocated space by flex ratio, while `Flexible` allows a child to be smaller than the allocated space if it doesn't need it all (depending on `fit: FlexFit.loose`) ⏎ C. Flexible can only be used inside GridView, not in Row/Column ⏎ D. Expanded only works with the Text widget
Answer: B. Expanded forces filling the entire allocated flex space; Flexible allows a smaller size if not needed (FlexFit.loose). A, C, D don't match their actual behavior.


Question 140. In Lab 6.3 (Tablet Layout Enhancement), the requirement for screens > 800px is to add a sidebar filter and switch to a 2-column movie list. What technique is recommended to implement this at the screen's root level? ⏎ A. Use `Wrap` alone, no other tool needed ⏎ B. Use `LayoutBuilder` at the root level to detect the width constraint, deciding whether to show the tablet layout (sidebar + 2 columns) or the mobile layout ⏎ C. Use `OrientationBuilder` instead of checking width ⏎ D. It's impossible to build a distinct tablet layout in Flutter
Answer: B. Using LayoutBuilder at the root level to detect the width constraint is the recommended technique (per the note "use LayoutBuilder at root level"). A, C, D don't match the recommended technique.


Question 141. What problem does the `AspectRatio` widget in the "Responsive Widgets Overview" list help solve? ⏎ A. Helps images avoid being stretched/distorted when the container size changes, by maintaining a fixed aspect ratio ⏎ B. Speeds up loading images from the network ⏎ C. Only usable for video, not for images ⏎ D. Fully replaces BoxFit
Answer: A. AspectRatio maintains a fixed aspect ratio so images aren't stretched when the container size changes. B, C, D don't match its scope/function.


Question 142. Consider: a sort Dropdown is placed inside a `Row` with `mainAxisSize: MainAxisSize.max` alongside other large fixed-size widgets, causing the total width to exceed a small screen. In the spirit of Module 6, what is the most reasonable solution? ⏎ A. Completely remove the Dropdown from small screens ⏎ B. Wrap the flexible element (like the Dropdown or long text) with `Flexible`/`Expanded`, or use `Wrap` to auto-wrap to a new line when there isn't enough space ⏎ C. Always give every element in the Row an absolute fixed size ⏎ D. Convert the entire Row into a Stack
Answer: B. Wrapping the flexible element with Flexible/Expanded or using Wrap for auto-wrapping is the recommended solution. A, C, D don't match the responsive principles taught.


Question 143. Per Module 6's "Best Practices," when should `Wrap` be prioritized over `Row`? ⏎ A. When displaying a long chip list that could overflow horizontal space ⏎ B. When there's only exactly 1 child element ⏎ C. When you need a fixed widget that never changes size ⏎ D. `Wrap` should never be prioritized over Row
Answer: A. Wrap should be prioritized for long chip lists that could overflow horizontally. B, C, D don't match the stated best practice.


Question 144. Comparing using `MediaQuery.of(context).size.width` at the full-screen level versus `LayoutBuilder` at a deeply nested child widget level (e.g. inside a narrow Card within a 3-column Row). Which statement is most accurate? ⏎ A. Both approaches always give identical results in every case ⏎ B. `MediaQuery` returns the device's full screen width even though the child widget is much more constrained by its parent, while `LayoutBuilder` accurately reflects the real space (constraint) the child widget actually has — so use LayoutBuilder when you need to respond to local space ⏎ C. LayoutBuilder cannot be used nested inside a Row ⏎ D. MediaQuery only works inside a StatelessWidget
Answer: B. MediaQuery returns full-screen width regardless of local constraints; LayoutBuilder accurately reflects the actual local space — use LayoutBuilder for local responsiveness. A, C, D don't match the analyzed technical characteristics.


Question 145. According to the lecture, which tool in Flutter Inspector helps debug responsive layout? ⏎ A. Layout Explorer ⏎ B. Network Inspector ⏎ C. Memory Profiler ⏎ D. CPU Profiler
Answer: A. "Layout Explorer" in Flutter Inspector helps debug responsive layout, per Module 6's "Best Practices." B, C, D are not the tool stated for this purpose.


Question 146. For an app running on small phones, large phones, tablets, and desktop, why does the lecture recommend "Avoid fixed values"? ⏎ A. Because Flutter doesn't support fixed numbers in code ⏎ B. Because fixed values (e.g. absolute pixel width/height) don't adapt to diverse screen sizes, easily causing overflow or wasted space on other devices ⏎ C. Because fixed values increase the APK file size ⏎ D. No specific technical reason
Answer: B. Fixed values don't adapt to diverse screen sizes, easily causing overflow or wasted space. A, C, D don't match the actual technical reason.


Question 148. What is `FractionallySizedBox` in the responsive widget list used for? ⏎ A. Sizing a widget as a percentage relative to its parent widget, instead of a fixed pixel value ⏎ B. Only used for animations ⏎ C. Cropping rounded corners on a widget ⏎ D. Has no real function, purely illustrative in the slide
Answer: A. FractionallySizedBox sizes a widget as a percentage of its parent, instead of fixed pixels. B, C, D misdescribe its function.


Question 149. Why does the lecture encourage testing responsive UI across multiple device sizes (e.g. Pixel 2 → Pixel 7 → iPad) instead of just one single emulator? ⏎ A. Because Flutter requires testing on at least 3 devices to submit an assignment ⏎ B. Because layout behavior (especially breakpoints, Wrap, GridView) can vary significantly across screen sizes, and overflow/misalignment bugs often only appear at specific sizes ⏎ C. Because each device runs a different version of Dart ⏎ D. No real reason, it's just an optional suggestion
Answer: B. Layout behavior can vary significantly across screen sizes, and bugs often only surface at specific sizes, hence the need for varied device testing. A, C, D don't match the actual technical reason.


Question 151. When should you use `TextFormField` instead of a plain `TextField`? ⏎ A. When you don't need any special functionality ⏎ B. When the input needs to be inside a `Form` and needs validation functionality ⏎ C. `TextFormField` can only be used for Web ⏎ D. There's no difference between the two widgets
Answer: B. Use TextFormField when the input is inside a Form and needs validation. A, C, D misdescribe when to use it.


Question 152. What is `GlobalKey<FormState>` used for in a `Form`? ⏎ A. Only used to set the form's color ⏎ B. Allows accessing and controlling the Form's state from outside, e.g. calling `validate()`, `save()`, `reset()` on all fields in the form ⏎ C. Syntactically required but has no real effect ⏎ D. Used to encrypt the entered data
Answer: B. GlobalKey<FormState> allows accessing/controlling the Form's state externally via validate()/save()/reset(). A, C, D misdescribe its role.


Question 154. What is the standard signature of a validator function in `TextFormField`? ⏎ A. `void Function(String value)` ⏎ B. `String? Function(String? value)` ⏎ C. `bool Function(String value)` ⏎ D. `Future<String> Function(String? value)`
Answer: B. The standard validator signature is `String? Function(String? value)`. A, C, D are not the correct standard Flutter Form validator signature.


Question 155. If a validator function returns `null`, what does that mean? ⏎ A. The field is invalid, showing the error "null" ⏎ B. The field is valid, no error shown ⏎ C. The field has not been filled in by the user yet ⏎ D. The Form will automatically reset to its initial value
Answer: B. Returning null means the field is valid, no error shown. A, C, D misdescribe the meaning of a null return value.


Question 158. `AutovalidateMode` has three modes: `disabled`, `always`, and `onUserInteraction`. According to the lecture, which mode is recommended and why? ⏎ A. `always` — because showing errors right away helps users be more careful ⏎ B. `disabled` — because never showing errors keeps the UI always clean ⏎ C. `onUserInteraction` — because it avoids showing red errors right from the start (annoying) while still responding promptly once the user starts interacting with that field, balancing "too early" and "too late" ⏎ D. No mode is recommended, it's arbitrary
Answer: C. `onUserInteraction` is recommended because it balances not showing errors too early (annoying) and not too late (confusing). A, B, D don't match the recommended mode/reason.


Question 159. Per the "Error Messages & UX" section, which of the following error messages is a GOOD UX example? ⏎ A. "ERROR_400_INVALID_FIELD" ⏎ B. "Enter a valid email" ⏎ C. "Invalid!!!" ⏎ D. "Something went wrong somewhere"
Answer: B. "Enter a valid email" is a good UX example: short, clear, specific. A, C, D are bad examples (technical error code, vague, unspecific).


Question 160. What is `FocusNode` combined with `textInputAction: TextInputAction.next` and `onFieldSubmitted` used for? ⏎ A. Encrypting data entered into the field ⏎ B. Controlling the shift of focus to the next input field when the user presses "Next" on the keyboard, improving the mobile input experience ⏎ C. Completely hiding the keyboard when starting to type ⏎ D. Automatically filling in data from an API
Answer: B. This combination controls shifting focus to the next field when the user presses Next, improving mobile UX. A, C, D misdescribe its purpose.


Question 161. Consider the requirement: "Tap outside the form → keyboard hides." What is the common implementation approach mentioned in the lecture? ⏎ A. Call `Navigator.pop(context)` when the user taps outside ⏎ B. Wrap the form in a `GestureDetector` with `onTap` calling `FocusScope.of(context).unfocus()` (hiding the keyboard when tapping outside the input fields) ⏎ C. This cannot be done in Flutter ⏎ D. The keyboard can only be hidden with the physical Back button
Answer: B. Wrapping the form with GestureDetector calling FocusScope.of(context).unfocus() on tap is the common implementation. A, C, D don't match the taught standard approach.


Question 162. Why should a long form be wrapped in `ListView` or `SingleChildScrollView`? ⏎ A. To increase the app's loading speed ⏎ B. To avoid layout overflow errors when the virtual keyboard appears and covers part of the form on a small screen, while allowing scrolling to view all fields ⏎ C. Because Form is syntactically required to be inside a ListView ⏎ D. No specific reason
Answer: B. Wrapping in ListView/SingleChildScrollView avoids overflow when the keyboard covers part of the form on small screens, while allowing scrolling. A, C, D don't match the actual reason.


Question 164. In the example above, if the user enters the email `"taken123@test.com"`, what message does the app show after 2 seconds? ⏎ A. "Account created for taken123@test.com" ⏎ B. "This email is already taken" ⏎ C. No message is shown ⏎ D. The app will throw an exception because the email is invalid
Answer: B. Since the email starts with "taken", the logic `_email.toLowerCase().startsWith("taken")` shows "This email is already taken" after 2 seconds. A, C, D don't match the given code's logic.


Question 165. In the "From Form to Backend" flow, what is the correct order of the data-processing process? ⏎ A. Server response → Validation → POST Request → UI Update ⏎ B. UI Form → Local Validation → POST Request → Server processes → Response → UI Update ⏎ C. POST Request → UI Form → Validation → Response ⏎ D. UI Update → Validation → POST Request → UI Form
Answer: B. The correct order: UI Form → Local Validation → POST Request → Server processes → Response → UI Update. A, C, D reverse the correct sequence.


Question 166. Why is checking `value == null || value.trim().isEmpty` (instead of just `value.isEmpty`) important in a required-field validator? ⏎ A. Because `value` in a validator is always type `String` and can't be null, so a null check isn't needed ⏎ B. Because `value` has type `String?` (nullable) and the user could enter only whitespace; checking both null and the trimmed empty string prevents treating a whitespace-only string as valid ⏎ C. Because `trim()` automatically fixes spelling errors ⏎ D. There's no real difference at all
Answer: B. `value` is nullable (String?) and a whitespace-only entry needs the trim+empty check to avoid being treated as valid. A is wrong since value can be null; C, D misdescribe trim()'s function/the real difference.


Question 167. In Module 7's list of input widgets, when should you use `Checkbox`/`Switch`/`Radio` instead of `TextFormField`? ⏎ A. When the data is a boolean or discrete choice, not free-form text ⏎ B. When you need to enter an email address ⏎ C. When you need to enter a long password ⏎ D. There's no suitable case for using these widgets
Answer: A. Checkbox/Switch/Radio suit boolean or discrete choices, not free-form text. B, C, D are cases needing TextFormField instead.


Question 168. A student writes an email validator that only checks `text.contains("@")`, skipping the dot (`.`) check. Per the logic taught (`!text.contains("@") || !text.contains(".")`), what is the risk of omitting this check? ⏎ A. No risk, checking `@` alone is enough to determine an absolutely valid email ⏎ B. A string like `"abc@com"` (missing the domain's dot) would still be considered valid despite not matching the basic email format the lecture requires checking ⏎ C. Dart will automatically add a dot if missing ⏎ D. The app will show a compile error if the dot check is missing
Answer: B. Skipping the dot check lets malformed strings like "abc@com" pass as valid, despite not matching the taught basic email format. A is wrong since there's a clear risk; C, D don't match Dart's actual behavior.


Question 169. What does `formKey.currentState!.reset()` do? ⏎ A. Removes the entire Form from the widget tree ⏎ B. Resets all fields in the form to their initial values and clears any existing error messages ⏎ C. Sends the data to the server ⏎ D. Only applies to the password field
Answer: B. reset() resets all fields to their initial values and clears error messages. A, C, D misdescribe this function.


Question 170. What is the "Multi-step wizard" mentioned in "Form Layout Patterns"? ⏎ A. A type of screen-transition animation ⏎ B. A layout pattern that splits a long form into multiple smaller steps/screens, reducing the feeling of being overwhelmed by too many fields on one screen ⏎ C. Only applicable to e-commerce apps ⏎ D. Another name for GlobalKey
Answer: B. A multi-step wizard splits a long form into smaller steps/screens, reducing overwhelm. A, C, D misdescribe this concept.


Question 172. Per Demo 1 (Basic Signup Form), which fields are required and what are each field's specific validity conditions? ⏎ A. Only a valid Email is needed, other fields aren't required ⏎ B. Name (not empty), Email (contains @ and .), Password (at least 6 characters) — all required ⏎ C. Only Password with a minimum of 6 characters is needed ⏎ D. There are no specific constraints, just not being empty
Answer: B. Demo 1 requires: Name (not empty), Email (contains @ and .), Password (at least 6 chars) — all mandatory. A, C, D are incomplete/incorrect about the stated requirements.


Question 173. What is `obscureText: true` in `TextFormField` used for? ⏎ A. Completely hides the entire field from the UI ⏎ B. Masks the entered characters (e.g. showing dots) — typically used for password fields ⏎ C. Disables the ability to type into the field ⏎ D. Automatically encrypts the data before sending to the server
Answer: B. `obscureText: true` masks entered characters (e.g. shows dots), typically for password fields. A, C, D misdescribe this function.


Question 174. Real-world scenario: a signup form has `Password` requiring at least 8 characters with a digit, `Confirm Password` must match Password, and `AutovalidateMode.onUserInteraction` is enabled. When the user first types a valid 8-char password with a digit, then types a mismatched confirm password, then goes back and edits the password to a different value — what happens to the error message on the Confirm Password field? ⏎ A. The Confirm Password error message automatically disappears forever and never shows again ⏎ B. Since `AutovalidateMode.onUserInteraction` tracks interaction per field, the "Passwords do not match" error at Confirm Password can keep showing until Confirm Password is updated to match the new Password (since the form typically only re-validates the field currently being interacted with, not automatically re-validating other fields unless the whole form is re-triggered) ⏎ C. The entire For
Answer: B. Since onUserInteraction tracks per-field interaction, the Confirm Password error can persist until that field is updated to match, as other fields aren't auto-re-validated. A, C, D don't match autovalidate's actual behavior.


Question 175. In the combined demo example (Full Signup Demo), which feature is NOT mentioned as part of the combined demo? ⏎ A. Show/hide password ⏎ B. Async email check with a loading button ⏎ C. Two-way sync with an SQLite database directly in the form ⏎ D. Focus management (Next/Done)
Answer: C. Two-way SQLite sync directly in the form is NOT part of the Full Signup Demo (that belongs to Module 9, not Module 7). A, B, D are all features included in the Full Signup Demo.


Question 176. Suppose a validator uses regex to check a stricter email format (instead of just checking `@` and `.`), e.g. `RegExp(r'^[\w.-]+@[\w.-]+\.\w+$')`. What benefit and trade-off does upgrading from a simple `contains` check to regex bring? ⏎ A. No trade-off at all, regex is always perfect with no downsides ⏎ B. Regex checks the format much more precisely and strictly than `contains`, but increases code complexity, and a poorly written regex could unintentionally reject valid emails (false negative) or wrongly accept an incorrect format ⏎ C. Regex only works on Web, not on Android/iOS ⏎ D. Dart doesn't support RegExp in a validator
Answer: B. Regex offers stricter/precise format checking but adds complexity and risks false negatives/positives if written incorrectly. A, C, D don't match the real trade-offs of using RegExp in Dart.


Question 177. In REST API programming, which HTTP status code range indicates SUCCESS? ⏎ A. 200-299 ⏎ B. 400-499 ⏎ C. 500-599 ⏎ D. 100-199
Answer: A. 200-299 is the HTTP status range indicating success. B, C, D are client-error, server-error, or informational ranges.


Question 178. When JSON is received from an API in Flutter, what corresponding Dart data type does it have AFTER `json.decode()`? ⏎ A. Always type `String` ⏎ B. A JSON object corresponds to `Map<String, dynamic>`, a JSON array corresponds to `List<dynamic>` ⏎ C. Always type `int` ⏎ D. Dart cannot represent JSON in any form
Answer: B. JSON object → `Map<String, dynamic>`, JSON array → `List<dynamic>` after decoding. A, C, D misdescribe the resulting data types.


Question 180. What is `FutureBuilder` in Flutter used for? ⏎ A. Building static UI that never changes ⏎ B. Integrating a `Future` (async operation) into the widget tree, automatically rebuilding the UI based on states: waiting, error, or data ⏎ C. Only used to display images from the network ⏎ D. Fully replaces ListView
Answer: B. FutureBuilder integrates a Future into the widget tree, rebuilding automatically based on waiting/error/data states. A, C, D misdescribe its function.


Question 182. Why should you create a model class (e.g. `Post` with `fromJson`) instead of directly manipulating `Map<String, dynamic>` throughout the app? ⏎ A. Because Dart doesn't allow using Map directly in the UI ⏎ B. Model classes provide type safety, clear structure, and are easier to maintain and reuse than accessing directly via string keys, which is error-prone at runtime ⏎ C. Model classes make the app run twice as fast ⏎ D. No clear benefit, it just increases the number of files
Answer: B. Model classes provide type safety, clear structure, and easier maintenance/reuse than raw Map access. A, C, D don't match the real benefit stated.


Question 186. Which HTTP method is most suitable for sending new data to the server in order to create a new resource? ⏎ A. GET ⏎ B. POST ⏎ C. DELETE ⏎ D. HEAD
Answer: B. POST is suitable for sending new data to create a new resource. A, C, D don't match this HTTP method's purpose.


Question 187. In Module 8's "Common Pitfalls & Best Practices," how is calling an API directly inside `build()` regarded? ⏎ A. A best practice that should be applied ⏎ B. A common pitfall to avoid — since `build()` can be called repeatedly, leading to uncontrolled repeated API calls; you should call the API in `initState()` instead ⏎ C. The only way to call an API in Flutter ⏎ D. Only causes problems with POST, doesn't affect GET
Answer: B. Calling an API inside build() is a pitfall to avoid since build() can rerun repeatedly; call in initState() instead. A, C, D don't match the lecture's stated pitfall content.


Question 188. What is the main benefit of the "Service Layer Pattern" design (like `ApiService` in Demo 8.4)? ⏎ A. Helps Flutter automatically generate the user interface ⏎ B. Encapsulates all API-calling logic in a separate class, keeping UI (widget) code focused on display, while allowing the same service to be reused across multiple screens ⏎ C. Fully replaces FutureBuilder ⏎ D. Only has the effect of reducing app size
Answer: B. The Service Layer Pattern encapsulates API logic separately, keeps UI focused on display, and allows service reuse across screens. A, C, D don't match the real benefit.


Question 190. In the "From API to UI: Putting It All Together" flow, after the user successfully submits a form to create a new post (createPost), what does the lecture suggest for updating the displayed list? ⏎ A. The whole app must be restarted ⏎ B. Re-fetch (call the list API again) or optimistically update directly on the existing list ⏎ C. Nothing needs to be done, the list will automatically update with no intervention ⏎ D. Clear the entire device cache
Answer: B. The suggested approaches are re-fetching or an optimistic update on the existing list. A, C, D don't match the lecture's suggestions.


Question 191. What is the main reason "hardcoding URLs everywhere in the code" is listed as a pitfall? ⏎ A. Hardcoded URLs make the app run significantly slower ⏎ B. It makes it difficult to change the base URL (e.g. switching dev/production environments), easily leading to mistakes and duplicated code when it must be edited in multiple places ⏎ C. Dart doesn't allow declaring URL strings directly in code ⏎ D. Only affects security, unrelated to code maintenance
Answer: B. Hardcoded URLs make changing the base URL difficult and cause duplication/errors across the codebase. A, C, D don't match the actual reason stated.


Question 192. Comparing parsing `response.body` (JSON) directly in a widget's `build()` function versus parsing in a separate function (like `fetchPosts()` called from `initState()`). Which statement is most accurate regarding performance? ⏎ A. No performance difference between the two approaches ⏎ B. Parsing JSON in `build()` causes the heavy work (parsing) to be redone every time the widget rebuilds — which can happen very often — while parsing in a function called from `initState()` (with the result stored in a Future variable) only happens once per data load ⏎ C. build() always runs faster than initState() regardless of content ⏎ D. Where code is placed doesn't affect performance, only code organization
Answer: B. Parsing in build() repeats the heavy work on every rebuild, while parsing once from initState() (result cached in a Future) happens only once per load. A, C, D don't match the real performance analysis.


Question 193. According to the lecture, what are the three typical API data groups students can choose from for their project? ⏎ A. realtime, media, mock APIs ⏎ B. authentication, storage, testing APIs ⏎ C. Only JSONPlaceholder alone ⏎ D. Firebase, SQLite, GraphQL
Answer: A. The three typical groups: realtime, media, mock APIs — matching the "What is an API? What is HTTP?" slide. B, C, D don't match the stated content.


Question 195. In the lecture's "Steps to call an API in Flutter" process, which step happens RIGHT AFTER receiving the JSON response? ⏎ A. Immediately update the UI with no further processing ⏎ B. Decode JSON → convert into a Dart model ⏎ C. Re-send the request once more to confirm ⏎ D. Delete all old data on the device
Answer: B. Right after receiving the JSON response, the next step is decoding JSON → converting to a Dart model, per the stated process. A, C, D don't match the correct sequence.


Question 197. In the HTTP status code table, which range indicates a SERVER-SIDE error? ⏎ A. 200-299 ⏎ B. 300-399 ⏎ C. 400-499 ⏎ D. 500-599
Answer: D. 500-599 is the range for server-side errors. A is success; B is redirection (not emphasized separately in the lecture but not a server error); C is client-side errors.


Question 198. What happens if `response.statusCode` is not 200 but the code does NOT check this condition before calling `json.decode(response.body)`? ⏎ A. No issue at all, json.decode always succeeds with every response ⏎ B. A parsing error may occur (if the server returns an error HTML page or error text instead of valid JSON), or the program continues processing invalid data as if the request succeeded ⏎ C. The app will automatically retry the request ⏎ D. json.decode will safely return an empty list in every error case
Answer: B. Skipping the status check risks a parsing error on non-JSON error content, or processing invalid data as if it were a successful response. A, C, D don't match the actual consequence.


Question 199. Comparing using the plain `http` package versus a more advanced HTTP management package (e.g. Dio, not in the slides but askable at an analytical level): in the spirit of Module 8's "Service Layer Pattern," why does wrapping HTTP logic in `ApiService` make switching HTTP libraries later (if needed) easier? ⏎ A. Because every HTTP package has an entirely identical API ⏎ B. Because all networking logic is isolated in a single class; if the library needs to change, you only edit inside `ApiService` without touching every widget that consumes the data, minimizing risk and effort ⏎ C. No benefit at all, switching libraries always requires rewriting the entire app ⏎ D. The Service Layer Pattern only applies to the `http` package, not to other packages
Answer: B. Isolating networking logic in one class means switching libraries only requires editing ApiService, not every consuming widget, minimizing risk/effort. A, C, D don't match this software-architecture principle.


Question 200. In Demo 8.2, when the user taps an item in the post list to open the detail screen, how is the post data (`Post`) passed to `PostDetailScreen`? ⏎ A. Calling the API again to separately load the detail data ⏎ B. Directly passing the already-available `Post` object through `PostDetailScreen`'s constructor when navigating via `MaterialPageRoute` ⏎ C. Saving it to SharedPreferences then reading it back on the detail screen ⏎ D. Using a global variable to share the data
Answer: B. The Post object is passed directly through PostDetailScreen's constructor during navigation via MaterialPageRoute, per the Demo 8.2 code. A, C, D don't match the demo's actual implementation.


Question 202. What kind of data does `SharedPreferences` best suit? ⏎ A. Complex relational data with many linked tables ⏎ B. Simple key-value data like flags or settings ⏎ C. Large image files ⏎ D. Long-duration video
Answer: B. SharedPreferences best suits simple key-value data like flags or settings. A, C, D are not suitable cases for SharedPreferences.


Question 203. According to the "Types of Local Storage in Flutter" table, which case best fits using SQLite (sqflite)? ⏎ A. Storing a boolean flag "has onboarding been completed" ⏎ B. Storing structured relational data, e.g. a Todo app with many records needing querying, filtering, sorting ⏎ C. Storing only the light/dark theme ⏎ D. Storing a simple text string that needs no querying
Answer: B. SQLite best fits structured relational data needing query/filter/sort, like a Todo app. A, C, D fit better with SharedPreferences or a simple file.


Question 204. In the "SharedPreferences Support Across Environments" table, why doesn't DartPad (Flutter Mode) actually support SharedPreferences? ⏎ A. Because DartPad only supports the JavaScript language ⏎ B. Because DartPad has no access to real device storage (no native storage), so a mock class (like FakePrefs) is needed to simulate storage behavior in memory ⏎ C. Because SharedPreferences only works on iOS ⏎ D. Because DartPad doesn't support any Flutter widgets at all
Answer: B. DartPad has no access to real device storage, requiring a mock class to simulate in-memory storage. A, C, D don't match the actual technical reason.


Question 205. On Flutter Web, `SharedPreferences` operates based on which browser storage mechanism? ⏎ A. IndexedDB ⏎ B. Cookie ⏎ C. localStorage ⏎ D. SessionStorage
Answer: C. On Flutter Web, SharedPreferences maps to the browser's `localStorage`. A, B, D don't match the mechanism stated in the comparison table.


Question 207. The slide note "Note: data resets on reload (not persistent)" when using FakePrefs on DartPad hints at what limitation? ⏎ A. Data stored in FakePrefs persists forever even after reloading the page ⏎ B. Since FakePrefs only stores data in the RAM of the current session, the data is lost on hot-restart or page reload — unlike real SharedPreferences, which persists durably on the device ⏎ C. FakePrefs only works with the String data type ⏎ D. This is a Flutter bug that needs to be patched
Answer: B. FakePrefs only stores data in-memory for the current session, so it's lost on hot-restart/reload, unlike real durable SharedPreferences. A, C, D don't match this stated limitation.


Question 208. Which package is mentioned for determining the app's storage directory when working with local JSON files? ⏎ A. shared_preferences ⏎ B. path_provider ⏎ C. sqflite ⏎ D. http
Answer: B. `path_provider` is used to determine app storage directories for local JSON files. A, C, D are not the package for this purpose.


Question 210. How does SQFlite differ from SQLite, per the "Why not name it SQLite?" explanation? ⏎ A. SQFlite and SQLite are two different names for the exact same concept, no technical difference ⏎ B. SQLite is the actual storage engine, while SQFlite is a Flutter plugin/wrapper (written in Dart + native code) that lets Flutter communicate with SQLite — SQFlite is not a standalone database ⏎ C. SQFlite runs 10 times faster than SQLite ⏎ D. SQFlite only works on Web, SQLite only works on mobile
Answer: B. SQLite is the actual storage engine; SQFlite is Flutter's plugin/wrapper that bridges to it, not a standalone database. A, C, D don't match the explanation given.


Question 211. Why does the lecture emphasize "SQFlite doesn't work on Web"? ⏎ A. Because SQL syntax isn't supported in web browsers in general ⏎ B. Because SQFlite is a plugin that depends on native code (Android/iOS) to communicate with the OS's SQLite engine, while web browsers have no corresponding native SQLite engine for this plugin to bridge to ⏎ C. Because web browsers don't support any form of local storage ⏎ D. Because SQFlite requires an Internet connection to work
Answer: B. SQFlite depends on native code to bridge to the OS's SQLite engine, which browsers lack — hence no Web support. A, C, D don't match the actual technical reason.


Question 212. In SQFlite, which function is called to open or create a database, and how often should it be called? ⏎ A. `openDatabase()` — should only be called once (singleton/service) to avoid opening multiple duplicate connections ⏎ B. `createDatabase()` — should be called every time before querying ⏎ C. `initDatabase()` — call frequency doesn't matter ⏎ D. `connectDatabase()` — only usable with Web
Answer: A. `openDatabase()` should only be called once (singleton/service) to avoid duplicate connections. B, C, D are not the correct function name/recommended usage.


Question 213. In SQFlite, what data type does the `query()` function return, and what's the necessary next step in a real app? ⏎ A. Directly returns `List<Model>`, no further processing needed ⏎ B. Returns `List<Map<String, dynamic>>` — each Map represents one data row; you need to map each Map into corresponding Model objects (e.g. using `fromJson`/`fromMap`) for use in the app ⏎ C. Returns a raw JSON string that needs manual parsing with RegExp ⏎ D. Returns `Future<void>`, with no data returned
Answer: B. `query()` returns `List<Map<String, dynamic>>`, requiring mapping to Model objects for app use. A, C, D don't match the actual return type.


Question 214. In the SQL SELECT syntax reviewed in the lecture, which clause is used to FILTER data rows before returning the result? ⏎ A. ORDER BY ⏎ B. WHERE ⏎ C. LIMIT ⏎ D. SELECT
Answer: B. `WHERE` filters data rows before returning results. A, C, D serve other purposes (sorting, limiting, selecting columns).


Question 215. When is the `GROUP BY` clause combined with `HAVING` typically used? ⏎ A. When you need to sort results alphabetically ⏎ B. When you need to group rows by a criterion and apply a filter condition on aggregate functions like SUM, COUNT ⏎ C. When you need to limit the number of returned records (pagination) ⏎ D. When you need to rename a column in the query result
Answer: B. GROUP BY + HAVING are used for grouping rows and filtering on aggregate functions like SUM/COUNT. A, C, D don't match the purpose of these clauses.


Question 216. Given: a Todo app needs to display the task list paginated to avoid loading all data at once on mobile. Which SQL clause is most suitable for implementing pagination? ⏎ A. GROUP BY and HAVING ⏎ B. LIMIT and OFFSET ⏎ C. WHERE alone is sufficient ⏎ D. SQLite doesn't support pagination
Answer: B. `LIMIT` and `OFFSET` are the standard SQL syntax for pagination. A, C, D are not suitable/correct for this purpose.


Question 217. "Offline-First" is an app design strategy based on what principle? ⏎ A. The app only works with a stable Internet connection ⏎ B. Prioritize loading and displaying local data first, then sync with the server when a network connection is available, keeping the app usable offline ⏎ C. Never store data locally, always call the API every time data is needed ⏎ D. Only applicable to game apps
Answer: B. Offline-First prioritizes loading local data first, syncing with the server when online, keeping the app usable offline. A, C, D misdescribe this design principle.


Question 218. Per the "When to Use Which Storage?" table, if an app needs offline storage AND backend sync (offline-first + sync), what combined storage solution is recommended? ⏎ A. Only use SharedPreferences ⏎ B. SQLite (sqflite) combined with a Firebase/REST backend ⏎ C. Only use a local JSON file, no backend combination needed ⏎ D. There's no suitable solution in Flutter
Answer: B. The recommended combination for offline-first + sync is SQLite (sqflite) with a Firebase/REST backend. A, C, D don't match the table's recommendation.


Question 219. In Demo 9.3 (Offline Todo App), what role does the `key: ValueKey(todo.id)` attached to each list item play (directly related to performance and correctness when the list changes)? ⏎ A. Purely decorative for the interface ⏎ B. Helps Flutter accurately identify each item widget when the list changes order/adds/removes items, avoiding state confusion between items during rebuild (especially important for lists that can be reordered or have elements deleted) ⏎ C. Used to encrypt the todo data before saving to SQLite ⏎ D. Mandatory for SQLite to work, unrelated to Flutter's Key concept
Answer: B. ValueKey(todo.id) helps Flutter accurately identify each item widget across list changes, preventing state mix-ups. A, C, D misdescribe Key's role.


Question 220. An app needs to store "a list of short notes, exportable/importable as a file" but doesn't need complex queries (no WHERE, JOIN...). Per the storage recommendation table, what is the most reasonable option? ⏎ A. SharedPreferences, because it's the simplest in every case ⏎ B. Local JSON File (dart:io), because it fits simple list-structured data, needs no SQL, and directly supports export/import ⏎ C. SQLite, because it's always the best choice regardless of data complexity ⏎ D. Don't store locally at all, always call the API every time the app opens
Answer: B. Local JSON File fits simple list-structured data with no need for SQL and supports direct export/import. A, C, D don't match the best fit for this specific requirement.


Question 221. Why is SharedPreferences described as "NOT for complex objects or large data"? ⏎ A. Because SharedPreferences only supports storing integers ⏎ B. Because SharedPreferences is designed for storing simple key-value values (String, bool, int, double, List<String>), which is unsuitable and inefficient for complex-structured or large-volume data ⏎ C. Because SharedPreferences only works with a network connection ⏎ D. Because SharedPreferences automatically deletes data after 24 hours
Answer: B. SharedPreferences is designed for simple key-value data, unsuitable/inefficient for complex or large data. A, C, D don't match the real reason.


Question 222. In SharedPreferences' standard High-Level Flow: "Load storage instance → Read existing value → Update value based on user action → Save back to storage → Refresh the UI." If the "Refresh the UI" step (e.g. calling `setState()`) is skipped after saving the new value, what happens? ⏎ A. The value is still correctly saved to device storage (correct on the next app launch), but the current UI won't reflect the change immediately until another rebuild occurs ⏎ B. The value won't be saved to storage if this step is missing ⏎ C. The app will automatically crash ⏎ D. SharedPreferences will automatically call setState implicitly
Answer: A. The value is still correctly saved to storage, but the current UI won't reflect the change until another rebuild happens. B, C, D don't match SharedPreferences/Flutter's actual behavior.


Question 223. Which data type is NOT listed as a typical example suitable for Local Storage in "Why Local Storage?" ⏎ A. Dark mode settings ⏎ B. Recently viewed items ⏎ C. Real-time video call session token (real-time streaming data) ⏎ D. Onboarding flag ("first-time user?")
Answer: C. A real-time video call session token is NOT listed as a typical Local Storage example. A, B, D are all examples explicitly mentioned in the lecture.


Question 224. Suppose a student uses SQLite to store a favorites list BUT chooses to store the entire list as ONE single JSON string in a TEXT column, instead of splitting it into proper relational rows. In the spirit of "Why SQLite?" (structured data, sorting & filtering, relationships), what is the drawback of this approach? ⏎ A. No drawback, this is the most optimal approach ⏎ B. It loses SQLite's core advantage: you can't efficiently use WHERE/ORDER BY/GROUP BY on individual elements inside that JSON string, forcing you to parse the entire string into memory and filter with Dart code — reducing query performance and losing the ability to leverage data relationships ⏎ C. SQLite will refuse to store JSON-string-format data ⏎ D. This approach makes the app unable to work offline
Answer: B. Storing the entire list as one JSON string loses SQLite's core querying/relational advantages, forcing inefficient in-memory filtering. A, C, D don't match the real technical drawback.


Question 225. In Lab 9 (Offline-First Mini App), what part of the app is SharedPreferences suggested to be used for? ⏎ A. Storing the entire relationally-structured Todo list ⏎ B. Storing the app's theme state (Dark/Light mode) ⏎ C. Storing the data export/import JSON file ⏎ D. Storing multi-table relational data
Answer: B. SharedPreferences is suggested for storing the app's theme state (Dark/Light mode) in Lab 9. A, C, D fit better with SQLite/JSON file.


Question 226. Comparing the three storage types learned (SharedPreferences, JSON File, SQLite) against the offline-first principle "Store offline → Use offline → Sync online." When designing a notes app that will later sync to a server, which initial local storage choice makes future "Sync online" and "Resolve conflicts" easiest to implement, and why? ⏎ A. SharedPreferences, because it's the simplest to start coding with ⏎ B. SQLite, because its clear row/column data structure makes it easy to track changes (e.g. by id, timestamp) for syncing and resolving conflicts with the server, compared to managing a single JSON string blob in SharedPreferences or a JSON file with no mechanism to query/track changes per element ⏎ C. No local storage needed, always call the API every time the app opens to avoid conflicts ⏎ D. JSON File, because files automatically sync in real-time with the server
Answer: B. SQLite's clear row/column structure makes tracking changes (id, timestamp) for syncing/conflict resolution far easier than a JSON blob or file. A, C, D don't match the technical reasoning for future sync/conflict handling.


Question 227. What is the difference between Authentication and Authorization? ⏎ A. The two concepts are entirely identical ⏎ B. Authentication answers "Who are you?" (identity verification), Authorization answers "What are you allowed to do?" (access control) ⏎ C. Authorization always happens before Authentication ⏎ D. Authentication only applies to web, not mobile
Answer: B. Authentication verifies identity ("Who are you?"); Authorization determines permissions ("What can you do?"). A, C, D misdescribe/reverse the two concepts.


Question 228. In the high-level Authentication Flow, after the backend verifies valid login credentials, what does the system usually return to the app? ⏎ A. The user's password in encrypted form ⏎ B. A token (usually a JWT) for the app to store and use for subsequent requests ⏎ C. The entire user database ⏎ D. A list of other apps the user has installed
Answer: B. The backend returns a token (usually JWT) for the app to store and use for subsequent requests. A, C, D don't match the standard authentication flow.


Question 229. What three main parts does a JWT (JSON Web Token) consist of? ⏎ A. Username, Password, Email ⏎ B. Header, Payload, Signature ⏎ C. Request, Response, Body ⏎ D. Client, Server, Database
Answer: B. A JWT consists of Header, Payload, Signature. A, C, D don't match JWT's actual structure.


Question 230. Per the instructor's note "JWTs must not store passwords. Only identity claims," what is WRONG if a system stores a user's password directly in the JWT payload? ⏎ A. No problem at all since JWT is signed, making it always absolutely secure ⏎ B. A JWT is only signed, not encrypted by default — meaning the payload can be decoded and read by anyone holding the token (e.g. via base64 decode), so storing sensitive info like a password is a serious security vulnerability ⏎ C. A JWT cannot contain an object-type payload, only plain strings ⏎ D. This is a standard, industry-recommended practice
Answer: B. JWT is only signed (not encrypted), so its payload is readable by anyone with the token — storing passwords in it is a serious vulnerability. A, C, D don't match JWT's actual security properties.


Question 231. What is the difference between an Access Token and a Refresh Token, per the lecture? ⏎ A. The Access Token has a longer lifetime than the Refresh Token ⏎ B. The Access Token is usually short-lived (reducing risk if exposed) and sent with every request; the Refresh Token has stronger privileges, used to get a new Access Token when it expires, so it needs more secure storage (Secure Storage) ⏎ C. The two token types have completely identical functions, just different names ⏎ D. The Refresh Token is sent with every API request, while the Access Token is only used once
Answer: B. Access Token is short-lived and sent per request; Refresh Token has stronger privileges, used to renew the Access Token, needing more secure storage. A, C, D misdescribe the two tokens' characteristics.


Question 232. In Demo 10.1, why does the lecture choose "Mock Authentication" (simulated auth inside AuthService) instead of calling a real API from the start? ⏎ A. Because Mock Authentication is theoretically faster in computation ⏎ B. To ensure the demo works reliably in every learning environment (not dependent on network connectivity, since a real server could fail/timeout), suitable for the stage of learning basic concepts before moving to real API integration ⏎ C. Because Real APIs always require payment ⏎ D. Because Flutter doesn't support making real HTTP requests in mobile apps
Answer: B. Mock Authentication ensures reliable demos across environments regardless of network issues, suited for teaching basic concepts before real API integration. A, C, D don't match the actual reason.


Question 233. Per the Mock API vs Real API comparison table, what is a disadvantage of Real API compared to Mock API in the teaching context? ⏎ A. Real API is always faster and more stable than Mock API ⏎ B. Real API can encounter errors from network, server, TLS, or CORS — less stable than Mock API, which always works without needing the Internet ⏎ C. Real API cannot be used to teach students about networking ⏎ D. Mock API is never used in real-world projects or in teaching at all
Answer: B. Real API can fail due to network/server/TLS/CORS issues, less stable than Mock API which needs no Internet. A, C, D don't match the comparison table's actual content.


Question 234. In the Signup Flow process, which statement is correct about the relationship between AuthService used for Login (Demo 10.1) and Signup (Demo 10.2)? ⏎ A. Signup must create an entirely new service, completely separate from Login's AuthService ⏎ B. Signup reuses the same `AuthService` class, only adding a new method (`signup(...)`), keeping the same architecture and error handling already established by Login ⏎ C. Signup needs no service at all, handling everything directly in the widget ⏎ D. AuthService is only used for logout
Answer: B. Signup reuses the same AuthService class, adding a new signup() method while keeping the existing architecture/error handling. A, C, D don't match the "Signup API Integration (Reuse AuthService)" content.


Question 235. Consider the password validation rules in the Signup Form: minimum 6-8 characters, at least 1 uppercase letter, at least 1 digit, no leading/trailing whitespace, and must match Confirm Password. With Password = `" Abc1234"` (leading space) and Confirm Password = `" Abc1234"` (identical, matching), what is the validation result per these rules? ⏎ A. Valid because Password and Confirm Password match completely ⏎ B. Invalid, because it violates the "No leading / trailing whitespace" rule even though the two values match ⏎ C. Valid because whitespace doesn't affect the password validation rules ⏎ D. Undetermined because Dart doesn't allow passwords to contain whitespace
Answer: B. Despite matching, it's invalid because it violates the "no leading/trailing whitespace" rule. A, C, D don't apply all the stated validation rules correctly.


Question 236. What is `SharedPreferences` used for in Demo 10.3, mainly? ⏎ A. Storing the user's entire web browsing history ⏎ B. Persisting the authentication token to implement auto-login, so the user doesn't need to log in again every time the app opens ⏎ C. Storing the user's avatar image as binary data ⏎ D. Fully replacing JWT
Answer: B. SharedPreferences persists the auth token for auto-login, avoiding repeated logins. A, C, D don't match its purpose in Demo 10.3.


Question 237. In the Auto Login Flow: "Splash → (has token?) → Home / Login." If the token has expired but still exists in SharedPreferences (the app only checks "does a token exist" WITHOUT checking its expiry), what could happen? ⏎ A. The app will always accurately detect an expired token because SharedPreferences auto-validates it ⏎ B. The app might mistakenly navigate the user to Home even though the token has expired, causing subsequent API requests to be rejected (401 Unauthorized) — showing the need to also check token expiry or handle 401 errors to force logout when needed ⏎ C. The token will automatically refresh with no additional logic needed ⏎ D. The app will always stay on the Splash screen forever
Answer: B. Without checking expiry, the app might wrongly navigate to Home, leading to 401 errors on subsequent requests, showing the need for expiry checks or 401 handling. A, C, D don't match the realistic consequence.


Question 238. In the "Logout & Session Clearing" Test Scenario of Demo 10.3, what specific action happens to the token data after the user logs out? ⏎ A. The token switches to an "expired" state but is still kept in memory ⏎ B. The token is removed from SharedPreferences, causing the next app launch to show the Login screen instead of automatically going to Home ⏎ C. The token is sent back to the server for permanent storage ⏎ D. Nothing changes, logout only temporarily hides the Home UI
Answer: B. Logout removes the token from SharedPreferences, so the next launch shows Login instead of auto-navigating to Home. A, C, D don't match the described test scenario behavior.


Question 239. What main benefit does Firebase Authentication provide over building your own authentication system from scratch? ⏎ A. It requires writing a more complex custom backend ⏎ B. Provides a secure, scalable login solution, supporting multiple methods (Google, Apple, Facebook, Email/Password) along with automatic session persistence and token refresh, reducing the effort of building an identity backend ⏎ C. Only works for apps that don't need login ⏎ D. Not compatible with Flutter
Answer: B. Firebase Auth provides secure, scalable, multi-method login with automatic session persistence/refresh, reducing backend identity effort. A, C, D don't match Firebase Authentication's real benefit.


Question 240. In the Google Sign-In flow: "User taps Sign in with Google → Google account chooser → Google issues OAuth tokens → Convert to Firebase credential → Firebase signs in the user → App receives Firebase User object." What is the core meaning of the statement "Firebase handles identity → our app only handles UI + navigation"? ⏎ A. The Flutter app must write all the OAuth authentication logic itself from scratch ⏎ B. Firebase handles all the complex parts of identity verification and OAuth token management; the Flutter developer only needs to focus on building the UI and handling navigation after a successful login ⏎ C. Google Sign-In is unrelated to Firebase at all ⏎ D. UI and navigation must be entirely handled on the server side
Answer: B. Firebase handles all the complex identity/OAuth verification, letting the Flutter developer focus only on UI and navigation. A, C, D don't match the meaning of this statement.


Question 241. Per the Google Sign-In "Common Errors & Fixes" table, the "DEVELOPER_ERROR" is usually caused by what? ⏎ A. Missing Internet connection ⏎ B. Wrong package name not matching the Firebase configuration ⏎ C. Missing pubspec.yaml file ⏎ D. Too-new Flutter version
Answer: B. "DEVELOPER_ERROR" is usually caused by a wrong package name not matching the Firebase configuration. A, C, D don't match the stated common cause.


Question 242. In the common errors table, if Google Sign-In returns null when tested on an emulator, what is the most common cause stated? ⏎ A. Missing camera permission ⏎ B. The emulator lacks Google Play Services — use an emulator with the Play Store installed or a real device instead ⏎ C. Wrong SharedPreferences configuration ⏎ D. Expired JWT
Answer: B. The emulator lacking Google Play Services is the common cause; use one with Play Store or a real device. A, C, D don't match the stated cause.


Question 243. What is the difference between Local Notification and Push Notification? ⏎ A. Local Notification is triggered by the app itself and needs no Internet; Push Notification is sent from a server and needs Internet plus backend support ⏎ B. The two notification types are completely identical in mechanism ⏎ C. Push Notification needs no backend, only Local Notification does ⏎ D. Local Notification only works on iOS
Answer: A. Local Notification is app-triggered, needing no Internet; Push Notification is server-sent, needing Internet and backend support. B, C, D misdescribe/reverse these two notification types.


Question 244. In Demo 10.5, at what point in the app's actual flow is the local notification triggered? ⏎ A. Right when the app opens, before login ⏎ B. Right after a successful login ⏎ C. Only when the app is fully in the background ⏎ D. When the app is uninstalled
Answer: B. The local notification is triggered right after a successful login, per Demo 10.5. A, C, D don't match the described real flow.


Question 245. Per "Best Practices & Patterns for Authentication," why is `pushReplacementNamed()` recommended over `pushNamed()` when moving from the Splash/Login screen to Home after successful authentication? ⏎ A. Because `pushReplacementNamed()` runs faster in CPU speed ⏎ B. To prevent the user from pressing Back to return to the Splash or Login screen that has already served its purpose, avoiding an illogical navigation experience (login → back → login again) ⏎ C. Because `pushNamed()` doesn't support named routes ⏎ D. There's no real difference between the two methods in this context
Answer: B. pushReplacementNamed() prevents returning to the finished Splash/Login screen via Back, avoiding illogical navigation. A, C, D don't match the actual reason.


Question 246. Per the "Security" section of Best Practices, what practice is recommended regarding token storage? ⏎ A. Store the token as public plain text in a log file for easy debugging ⏎ B. Don't store raw tokens insecurely — use a secure storage mechanism and always use HTTPS ⏎ C. Send the token over an unencrypted HTTP channel to increase speed ⏎ D. Share the token via social media to sync across multiple devices
Answer: B. The recommended practice is secure storage and always using HTTPS, never insecurely storing raw tokens. A, C, D are WRONG practices, contrary to the recommended security guidance.


Question 247. An app does the following: (1) The user logs in and receives an Access Token that expires in 15 minutes; (2) The Access Token is stored in SharedPreferences; (3) No Refresh Token mechanism is implemented. What is the most likely real-world consequence after 15 minutes of continuous app use? ⏎ A. The app will automatically extend the token forever with no action needed ⏎ B. API requests after the expiry point will start being rejected (401 Unauthorized), and since there's no Refresh Token, the user is forced to manually log in again to get a new Access Token — this is a limitation that should be fixed with a refresh token mechanism ⏎ C. The Access Token will automatically become a Refresh Token ⏎ D. The app will crash immediately when the Access Token expires
Answer: B. Without a refresh mechanism, requests after expiry get 401 errors, forcing manual re-login — a limitation needing a refresh token fix. A, C, D don't match the realistic consequence.


Question 248. In "Required Configurations" for Google Sign-In on Android, which file needs to be downloaded and placed in the `android/app/` folder? ⏎ A. Info.plist ⏎ B. google-services.json ⏎ C. build.gradle.template ⏎ D. AndroidManifest.template
Answer: B. `google-services.json` needs to be downloaded and placed in `android/app/` for Firebase configuration on Android. A, C, D are not the correct file name/purpose.


Question 249. Per the "Understanding User Session" section, what components does a typical session usually include? ⏎ A. Only the user's password ⏎ B. Access token, user profile info, and the token expiry timestamp ⏎ C. Only the app name and Flutter version ⏎ D. The user's entire operation history on the device
Answer: B. A typical session includes: access token, user profile, and token expiry timestamp. A, C, D are incomplete/incorrect about the content of "Understanding User Session."


Question 250. Compare the AuthService (Singleton pattern) mentioned in "Architecture Patterns" with scattering authentication logic directly across individual screen widgets (LoginScreen, SignupScreen, HomeScreen each writing their own API-calling logic). In the spirit of the lecture, what is the main drawback of the scattered approach? ⏎ A. No drawback at all, both approaches are equivalent in quality ⏎ B. The scattered approach leads to duplicated logic across screens, is hard to maintain consistently when the auth flow changes, and reduces reusability — while a centralized AuthService separates UI from business logic, making it easier to test and extend ⏎ C. The scattered approach is always faster at runtime ⏎ D. The Singleton AuthService is only suitable for apps that don't need login
Answer: B. Scattering auth logic causes duplication, hard maintenance, and low reusability; a centralized AuthService separates UI from logic, aiding testability/extensibility. A, C, D don't match the software architecture analysis.


Question 251. Per "Why Notifications After Authentication?", what is the main purpose of sending a notification right after a successful login (e.g. "Login successful. Welcome back!")? ⏎ A. Fully replaces showing a SnackBar ⏎ B. Connects notifications with the app's real business workflows (like confirming an important action that just occurred), increasing user engagement and awareness of their account status ⏎ C. Only meant to check the device's Internet access permission ⏎ D. Mandatory for the JWT to become valid
Answer: B. The purpose is connecting notifications with real business workflows, boosting engagement and account-status awareness. A, C, D don't match the actual purpose stated.


Question 252. What are the three main testing types in Flutter mentioned in the lecture? ⏎ A. Unit Test, Widget Test, Integration Test ⏎ B. Alpha Test, Beta Test, Release Test ⏎ C. Manual Test, Auto Test, Smoke Test ⏎ D. Load Test, Stress Test, Security Test
Answer: A. The three main testing types: Unit Test, Widget Test, Integration Test. B, C, D are not the testing classifications taught in Module 11.


Question 253. Per the "Flutter Testing Pyramid," which test type should have the LARGEST quantity in a project, since it's fast, stable, and low cost? ⏎ A. Integration Test ⏎ B. Widget Test ⏎ C. Unit Test ⏎ D. All three types should have equal quantities
Answer: C. Unit Tests should be the most numerous per the Testing Pyramid — fast, stable, low cost. A, B are not the correct position in the pyramid; D is wrong since the distribution is not equal.


Question 254. In the testing pyramid, how many Integration Tests are recommended and for what purpose? ⏎ A. Use the most since it's the most comprehensive test type ⏎ B. Use few, since they run slowly and cost more — should only be applied to critical, business-critical user flows ⏎ C. Integration Tests should never be used under any circumstance ⏎ D. Only used to test CPU performance
Answer: B. Integration Tests should be used sparingly since they're slow/costly — reserved for critical user flows. A, C, D don't match the Testing Pyramid's principle.


Question 256. How does a Widget Test in Flutter differ from a Unit Test? ⏎ A. Widget Test only checks pure logic unrelated to UI, exactly like a Unit Test ⏎ B. Widget Test checks UI rendering and interaction (e.g. entering text, tapping buttons) on a widget or small widget tree, while Unit Test only checks logic/functions unrelated to UI ⏎ C. Widget Test always runs slower than Integration Test ⏎ D. Widget Test cannot simulate typing text or tapping
Answer: B. Widget Test checks UI rendering/interaction on a widget tree; Unit Test checks pure UI-unrelated logic. A, C, D misdescribe the two test types' characteristics.


Question 258. In a Navigation Test, how does `pumpAndSettle()` differ from a plain `pump()`? ⏎ A. No difference at all, the two functions are entirely equivalent ⏎ B. `pumpAndSettle()` continuously "pumps" frames until no animation/transition is still running (fully settled), very useful for waiting for a screen-transition effect to finish before asserting ⏎ C. `pumpAndSettle()` can only be used for Unit Test ⏎ D. `pump()` always waits longer than `pumpAndSettle()`
Answer: B. pumpAndSettle() continuously pumps frames until all animations settle, useful for waiting out navigation transitions. A, C, D misdescribe this function.


Question 259. In Demo 11.4 (Integration Test: Full CRUD Flow), the test sequentially: adds a task → opens detail → edits the title → saves → checks the updated list. Why is this test type classified as "Integration Test" rather than a plain "Widget Test"? ⏎ A. Because it uses more lines of code than a typical Widget Test ⏎ B. Because it validates the entire interaction flow across multiple app layers (UI → Provider → Repository) through consecutive steps, simulating a complete real user journey, rather than checking just a single isolated UI behavior ⏎ C. Because it doesn't use any widgets at all ⏎ D. Because Integration Test and Widget Test are the same concept, just different names
Answer: B. It's classified as Integration Test because it validates the full flow across UI → Provider → Repository, simulating a complete user journey. A, C, D don't match the classification reason.


Question 260. Per the "How Tests Map to Real Flutter Problems" table, the "Wrong navigation" issue should mainly be detected by which test type? ⏎ A. Unit Test ⏎ B. Navigation Test (part of the extended Widget Test group) ⏎ C. Navigation issues cannot be tested at all ⏎ D. Can only be detected via manual testing
Answer: B. "Wrong navigation" should mainly be caught by a Navigation Test (an extended Widget Test), per the table. A, C, D don't match the table's content.


Question 261. In the Taskly App's architecture (used as the running example throughout Module 11), which component is responsible for managing app-wide state? ⏎ A. TaskListScreen ⏎ B. TaskProvider ⏎ C. TaskRepository ⏎ D. main.dart
Answer: B. TaskProvider is responsible for managing app-wide state in Taskly's architecture. A, C, D don't match this responsibility.


Question 262. In Taskly App, `TaskRepository` handles CRUD logic for tasks, while `TaskProvider` manages state. If a Widget Test wants to check UI behavior WITHOUT depending on the Repository's real database/API connection, what reasonable approach is recommended in Module 11's "Best Practices"? ⏎ A. Always connect directly to real data to ensure absolute test accuracy ⏎ B. Mock the Repository when possible, letting Widget Tests run fast, reliably, and independently of real external data state ⏎ C. Completely skip testing any widget related to the Repository ⏎ D. Move all Repository logic inside the Widget to make testing easier
Answer: B. Mocking the Repository when possible keeps Widget Tests fast, stable, and independent of external data state. A, C, D don't match the recommended best practice.


Question 263. What is `find.byKey(const Key('taskTextField'))` used for in a test? ⏎ A. Searching for displayed text on screen by string content ⏎ B. Finding one specific widget exactly via its assigned Key, making widget identification in the test tree more reliable than searching by type or text (which can be duplicated or change) ⏎ C. Finding all TextFields on screen regardless of Key ⏎ D. Has no effect, purely illustrative
Answer: B. find.byKey() reliably locates one specific widget via its assigned Key, more reliable than searching by type/text. A, C, D misdescribe its function.


Question 264. Per Module 11's "Best Practices," what does the "Avoid testing Flutter framework functionality" item advise? ⏎ A. You should write a test to verify that `Column` actually arranges elements vertically ⏎ B. You should not write tests to re-verify behavior that the Flutter framework itself already guarantees works correctly (like the basic mechanism of built-in widgets); focus instead on testing your app's own business logic and custom behavior ⏎ C. You are not allowed to use any built-in Flutter widget in the app ⏎ D. The Flutter framework cannot be tested in any form
Answer: B. Don't re-test behavior already guaranteed by the framework itself; focus on the app's own business logic and custom behavior. A, C, D don't match the actual advice given.


Question 265. What debugging aspects does DevTools in Flutter support, per Module 11? ⏎ A. Only supports syntax debugging ⏎ B. Layout issues, failed state updates, slow performance, unnecessary rebuilds, and navigation issues ⏎ C. Only supports checking network connectivity ⏎ D. Only used to build the release app
Answer: B. DevTools supports layout issues, failed state updates, performance, unnecessary rebuilds, and navigation issues, per "Debugging Goals in Flutter." A, C, D don't cover its full scope.


Question 267. What is `expect()` in flutter_test used for? ⏎ A. Initializing the widget tree for the test ⏎ B. Comparing the actual value with the expected value, and failing if they don't match — this is the "Assert" step in the testing process ⏎ C. Waiting for an animation to finish ⏎ D. Generating random mock data
Answer: B. expect() compares actual vs. expected values, failing on mismatch — the Assert step. A, C, D misdescribe expect()'s function.


Question 268. Consider the lecture's explanation: "Most UI issues start from logic → unit tests catch problems early." Applying this practically: if the function counting incomplete tasks (`getIncompleteCount()`) in TaskProvider has a logic bug (calculates wrong), but the UI still shows the correct number by coincidence with small test data, which testing approach best and most reliably catches this bug early, regardless of data size? ⏎ A. Relying solely on manual UI observation with random datasets ⏎ B. Write a dedicated Unit Test for `getIncompleteCount()` with multiple different datasets (edge cases: empty, all completed, mixed), isolating and verifying the calculation logic correctly without depending on whether the UI "happens" to display correctly ⏎ C. Integration Test is the only way to catch this logic bug ⏎ D. No automated testing form can detect a logic bug
Answer: B. A dedicated Unit Test with varied edge-case datasets isolates and reliably verifies the calculation logic regardless of coincidental correct display. A, C, D don't match the most reliable early-detection approach.


Question 269. Why does the lecture recommend "Use keys for widget tests"? ⏎ A. Because a Key is syntactically mandatory for every Flutter widget ⏎ B. Because assigning a Key helps accurately locate a specific widget in the test, avoiding confusion when multiple widgets of the same type or text content exist on screen ⏎ C. Because a Key doubles the test's execution speed ⏎ D. A Key only works with StatelessWidget, not with StatefulWidget
Answer: B. Assigning a Key aids accurate widget location in tests, avoiding confusion among similar widgets. A, C, D don't match the reasoning behind this recommendation.


Question 270. What characteristic of the UI Overflow bug described in Module 11 makes it hard to detect if you only rely on running the app normally? ⏎ A. Overflow always makes the app crash instantly, so it's very easy to spot ⏎ B. Overflow bugs typically don't crash the app (no exception stopping the program), so tools like tests or DevTools are needed to detect them, rather than just relying on the app "still running" ⏎ C. Overflow only occurs on iOS devices ⏎ D. Overflow is always automatically fixed by Dart at compile time
Answer: B. Overflow bugs usually don't crash the app, requiring tests/DevTools for detection rather than just observing that it "still runs." A, C, D don't match the actual characteristic of overflow bugs.


Question 272. Per Module 11's Summary, which skill is NOT listed as a learning objective of this module? ⏎ A. Writing Unit Tests to check model logic and behavior ⏎ B. Writing Widget Tests to check UI rendering and interaction ⏎ C. Building and signing a release APK file for publishing to Google Play ⏎ D. Using DevTools to diagnose layout, rebuild, and navigation issues
Answer: C. Building and signing a release APK for Google Play publishing is a Module 12 topic, NOT a Module 11 learning objective. A, B, D are all accurate Module 11 learning objectives.


Question 273. A student writes an Integration Test but insists on also testing whether "the AppBar's background color is exactly Colors.blue per Material Design's default" (a default framework behavior, not custom app logic). Per the "Best Practices" learned, why should this approach be avoided? ⏎ A. Because Integration Test is not allowed to check colors in any form ⏎ B. Because this tests the Flutter framework's default behavior (not the app's custom business logic), wasting effort writing the test without adding any real value to ensuring app quality ⏎ C. Because AppBar cannot be tested by any tool ⏎ D. Because colors always change randomly between builds so they can't be tested
Answer: B. Testing a default framework behavior instead of custom app logic wastes effort without adding real quality-assurance value. A, C, D don't match the reasoning behind avoiding this.


Question 274. What does `tester.pumpWidget(createTaskListTestApp())` do as the first step of a Widget Test? ⏎ A. Clears all previous test data ⏎ B. "Pumps" (renders) the given widget tree into the virtual test environment, equivalent to launching the app for the first time to begin testing ⏎ C. Builds the app into an APK file ⏎ D. Connects to a real server to fetch data
Answer: B. pumpWidget() renders the given widget tree into the test environment, equivalent to launching the app to begin testing. A, C, D misdescribe this function.


Question 275. Consider: a student group writes VERY MANY Integration Tests (the vast majority of all tests) but almost no Unit Tests. Per the "Testing Pyramid" principle learned, what realistic consequence is likely? ⏎ A. No negative consequence at all, more Integration Tests is always better in every case ⏎ B. The test suite will run significantly slower, cost more time/resources to maintain, and pinpointing the root cause when a bug occurs will be harder than having a solid Unit Test foundation to isolate logic bugs early ⏎ C. Integration Tests are always faster than Unit Tests, so this is the optimal strategy ⏎ D. The distribution of test types has no effect on speed or maintenance cost at all
Answer: B. Too many Integration Tests and too few Unit Tests leads to a slower, costlier-to-maintain suite and harder root-cause isolation. A, C, D contradict the Testing Pyramid principle.


Question 276. In "Debugging Goals in Flutter," the "Frequent unnecessary rebuilds" issue directly relates to which topic that will be explored deeper in Module 12? ⏎ A. Building local JSON files ⏎ B. Performance Optimization ⏎ C. Deep link configuration ⏎ D. Setting up Firebase Authentication
Answer: B. "Frequent unnecessary rebuilds" directly relates to Performance Optimization, explored further in Module 12. A, C, D are unrelated to this rebuild issue.


Question 277. What frame-rate target (FPS) does Flutter aim for to ensure a smooth experience on standard devices? ⏎ A. 24 FPS ⏎ B. 30 FPS ⏎ C. 60 FPS ⏎ D. 144 FPS
Answer: C. Flutter targets 60 FPS for a smooth experience on standard devices. A, B, D don't match the stated number.


Question 278. If a device runs at 60 FPS, roughly how many milliseconds does Flutter have to complete build, layout, and paint for EACH frame before a dropped frame (jank) occurs? ⏎ A. About 16.6ms ⏎ B. About 100ms ⏎ C. About 1ms ⏎ D. About 1000ms
Answer: A. At 60 FPS, Flutter has about 16.6ms per frame. B, C, D don't match the calculated figure in the lecture.


Question 279. Per "How Flutter Builds the UI (Render Pipeline)," what are the respective roles of the three core concepts Widget, Element, RenderObject? ⏎ A. Widget is an immutable blueprint describing configuration, Element manages lifecycle & state, RenderObject performs the actual layout/painting/compositing ⏎ B. Widget, Element, RenderObject are three different names for the same concept ⏎ C. RenderObject is immutable, Widget performs painting directly to the screen ⏎ D. Element only exists in Widget Test, not when the app actually runs
Answer: A. Widget = immutable blueprint, Element = lifecycle/state manager, RenderObject = performs layout/painting/compositing. B, C, D misdescribe the roles of these three components.


Question 281. What benefit does the "Use Selector to rebuild ONLY the list" solution in Demo 12.1 bring over a plain `context.watch<T>()`? ⏎ A. Selector allows fine-grained rebuilding — only the UI portion actually dependent on the changed data rebuilds, instead of the entire parent widget as with a broad-scope watch ⏎ B. Selector and watch behave entirely identically regarding rebuilds ⏎ C. Selector only works with the String data type ⏎ D. Selector causes the entire app to always rebuild regardless of whether anything changed
Answer: A. Selector enables fine-grained rebuilds, only rebuilding the part actually dependent on changed data. B, C, D don't match Selector's real behavior/benefit.


Question 282. How does the `const` keyword before a widget (e.g. `const Text("Task Title")`) technically optimize performance? ⏎ A. `const` only makes the code look tidier, no runtime effect ⏎ B. A widget marked `const` is fully immutable at compile-time; Flutter can recognize it as the same instance across rebuilds and skip rebuilding it (more efficient tree diffing), saving memory and reducing rebuild cost ⏎ C. `const` causes the widget to always be redrawn every frame to ensure sync ⏎ D. `const` can only be applied to StatefulWidgets
Answer: B. const widgets are compile-time immutable, letting Flutter skip rebuilding them and diff more efficiently, saving memory/rebuild cost. A, C, D don't match const's real technical optimization.


Question 283. Per "Optimization Technique 3: Avoid Heavy Work in build()," which of the following should NOT be done directly inside the `build()` function? ⏎ A. Returning a simple `Text` widget ⏎ B. Calling an API, parsing JSON, sorting a list, or doing complex calculations ⏎ C. Returning a `Column` containing a few static child widgets ⏎ D. Reading a value from MediaQuery
Answer: B. Calling APIs, parsing JSON, sorting, or complex calculations should NOT be done inside build(). A, C, D are normal, low-cost operations that don't cause significant performance issues in build().


Question 285. Per "Optimization Technique 4: Using Keys Properly," why is using `UniqueKey()` for each list item considered "Bad"? ⏎ A. `UniqueKey()` is syntactically invalid in Dart ⏎ B. `UniqueKey()` generates an entirely NEW Key on every build, making Flutter always treat the widget as a "new widget" and forcing a full rebuild instead of reusing/updating the old widget — the exact opposite of Key's performance-optimization purpose ⏎ C. `UniqueKey()` only works with the int data type ⏎ D. `UniqueKey()` causes the app to crash immediately
Answer: B. UniqueKey() generates a new Key every build, forcing unnecessary full rebuilds instead of reuse — the opposite of Key's optimization purpose. A, C, D don't match the real reason it's considered bad.


Question 286. Compare `ValueKey(task.id)` with `UniqueKey()` in the context of a task list that can be deleted/added/reordered. Why is `ValueKey(task.id)` recommended ("Good")? ⏎ A. Because `ValueKey` is always random just like `UniqueKey`, no real difference ⏎ B. Because `ValueKey(task.id)` ties the Key to a stable identifier value (id) of each task — Flutter can use it to accurately identify exactly which widget corresponds to which data across rebuilds, even when the list order changes, helping preserve state and optimize diffing ⏎ C. `ValueKey` only works with lists of fewer than 10 elements ⏎ D. `ValueKey` and using no Key at all (default null Key) always give identical performance results
Answer: B. ValueKey ties to a stable id, letting Flutter accurately match widgets to data across rebuilds/reorders, preserving state and optimizing diffing. A, C, D don't match ValueKey's real characteristics/benefit.


Question 287. Why does the lecture recommend running Flutter DevTools in "Profile Mode" instead of "Debug Mode" when measuring performance? ⏎ A. Because DevTools cannot be opened in Debug Mode ⏎ B. Because Debug Mode has a lot of debugging overhead (assertions, extra checks) that make measured performance numbers not accurately reflect real release-mode performance; Profile Mode is closer to real-world performance ⏎ C. Because Profile Mode runs slower than Debug Mode, making it suitable for finding bugs more thoroughly ⏎ D. There's no difference between the two modes when measuring performance
Answer: B. Debug Mode's overhead skews performance numbers away from real release performance; Profile Mode is closer to reality. A, C, D don't match the actual reason for this recommendation.


Question 288. In "Identifying Rebuild Issues," how should the statement "If state lives too high → entire UI rebuilds" be understood in the context of widget architecture design? ⏎ A. Placing state as high as possible in the widget tree is always the best way to optimize performance ⏎ B. If state is placed on an ancestor widget too high in the tree (covering many unrelated UI parts), any change to that state triggers a rebuild of the entire subtree below it, including UI parts that don't actually depend on the changed data ⏎ C. Where state is placed in the widget tree has no effect on rebuild scope ⏎ D. State should only ever be placed at the app's root widget in every case
Answer: B. Placing state too high causes an overly broad rebuild scope, affecting even unrelated UI parts below it. A, C, D don't match this architectural principle.


Question 289. Per "Optimization Technique 2: Extract Widgets" (e.g. extracting `TaskTile` as its own widget), what is the main benefit of breaking a large widget into smaller pieces? ⏎ A. Significantly shrinks the APK file size ⏎ B. When one item's data changes, only the corresponding child widget (like a specific `TaskTile`) needs to rebuild, instead of the entire list/parent screen rebuilding along with it, reducing rebuild scope and cost ⏎ C. An extracted widget automatically gets its own key without needing to declare one ⏎ D. No real benefit, just makes the code look tidier formally
Answer: B. Extracting widgets narrows rebuild scope to just the changed item, reducing overall rebuild cost. A, C, D don't match the real technical benefit.


Question 290. In "Demo 12.2 - Image Optimization," what does the `precacheImage()` technique do? ⏎ A. Removes an image from memory as soon as it's no longer used ⏎ B. Pre-loads an image into the cache before it's actually needed for display, reducing lag when the image first appears on the UI ⏎ C. Automatically compresses the image to a smaller format ⏎ D. Only works with images fetched from the network, not with asset images
Answer: B. precacheImage() pre-loads an image into cache before display, reducing lag on first appearance. A, C, D misdescribe this function.


Question 291. Why does the lecture recommend resizing images before use, e.g. avoiding loading a 2000px image for a thumbnail that only displays at 50px? ⏎ A. Because Flutter doesn't support displaying images larger than 100px resolution ⏎ B. Because decoding and storing in memory an image with a resolution far larger than the actual display size wastes significant memory and processing time, even though the final displayed result is just a small image ⏎ C. Large images are always automatically rendered at low quality by Flutter regardless of original size ⏎ D. No effect on performance at all, only affects server-side storage size
Answer: B. Decoding/storing an oversized image wastes significant memory and processing time relative to the small display result. A, C, D don't match the actual performance impact.


Question 292. Which command is used to analyze a detailed app-size breakdown by source code, assets, and packages? ⏎ A. flutter doctor ⏎ B. flutter build apk --analyze-size ⏎ C. flutter test ⏎ D. flutter clean
Answer: B. `flutter build apk --analyze-size` is used for a detailed app-size breakdown. A, C, D are not the correct command for this purpose.


Question 293. Per "Common Size Optimization Tips," what benefit does "Enable tree-shaking icons" (removing unused Material icons) bring to the released app? ⏎ A. Makes icons look visually nicer ⏎ B. Removes Material Design icons not actually used in the app from the final build package, reducing the app's size ⏎ C. Tree-shaking only affects app runtime speed, not size ⏎ D. This technique only applies to images, not icons
Answer: B. Tree-shaking icons removes unused Material icons from the build, reducing app size. A, C, D don't match the actual benefit.


Question 294. What is the difference between `flutter build apk --debug` and `flutter build apk --release`? ⏎ A. No difference at all in performance or size ⏎ B. The debug build contains extensive debugging support info, not optimized for performance/size; the release build is optimized, removes debug overhead, giving performance and size closer to the real end-user experience ⏎ C. `--release` only works on iOS, `--debug` only works on Android ⏎ D. `--debug` always produces a smaller file size than `--release`
Answer: B. Debug builds carry unoptimized debugging overhead; release builds are optimized for real-world performance and size. A, C, D don't match the real difference between the two build modes.


Question 295. What file format does Google Play require (instead of a traditional APK) when publishing an app to the store, and what is the corresponding build command? ⏎ A. A .aab file (Android App Bundle), built with the command `flutter build appbundle --release` ⏎ B. A .ipa file, built with the command `flutter build ios --release` ⏎ C. A .exe file, built with the command `flutter build windows --release` ⏎ D. Google Play only accepts .apk files, no other format
Answer: A. Google Play requires .aab format, built via `flutter build appbundle --release`. B, C, D don't match the correct format/command for Android on Google Play.


Question 296. What important rule governs `versionCode` in `android/app/build.gradle` when publishing consecutive updates? ⏎ A. It must always keep the same value across every release ⏎ B. It must be an integer and must increase with every new release (every update pushed to the store) ⏎ C. It doesn't matter, it can be any value including decreasing ⏎ D. Only applies to apps running on iOS
Answer: B. versionCode must be an integer and increase with each new release. A, C, D don't match this required versioning rule.


Question 297. Before running `flutter build apk --release` for the official release, the lecture recommends first trying the app in "Profile Mode" (`flutter run --profile`). What is the most reasonable reason for this intermediate step? ⏎ A. Because Profile Mode is the only mandatory step to successfully build the release file ⏎ B. Profile Mode removes most of Debug Mode's overhead while still allowing observation of near-real-world performance (smoothness/jank, build/render speed) before creating the official release build, helping catch performance issues early without waiting until the final packaging step ⏎ C. Profile Mode and Release Mode are entirely identical so this step is unnecessary ⏎ D. Profile Mode is only used to check syntax errors, unrelated to performance
Answer: B. Profile Mode removes most Debug overhead while showing near-real performance, catching issues early before the final release build. A, C, D don't match the actual reasoning for this step.


Question 298. In the "Deployment Checklist" after completing Demo 12.4, which of the following items is mentioned? ⏎ A. Remove debug statements, clean up unused assets, ensure versionCode has been updated ⏎ B. Double the number of animations in the app ⏎ C. Delete the entire pubspec.yaml configuration file ⏎ D. Convert the entire app to use Widget Test instead of Unit Test
Answer: A. The checklist mentions removing debug statements, cleaning unused assets, and ensuring versionCode is updated. B, C, D are not reasonable/stated items in the actual checklist.


Question 299. A student notices the Taskly app is janky when scrolling a long list (ListView). After simultaneously applying three techniques: (1) extracting `TaskTile` into its own widget with `const` where possible, (2) using `Selector` instead of `context.watch` at the full-screen level, (3) assigning `ValueKey(task.id)` to each item. In the spirit of the techniques taught in Module 12, how do these three changes work together to reduce scroll jank? ⏎ A. These three techniques are unrelated to each other and only one of them actually has an effect ⏎ B. Widget extraction + const reduces the rebuild cost of each item when unnecessary; Selector narrows the scope of widgets rebuilt when data changes (only the list portion instead of the whole screen); and ValueKey helps Flutter correctly identify each item when the list changes to avoid mistaken rebuilds/state loss — the three techniques complement each other to reduc
Answer: B. The three techniques complement each other: extraction/const reduces per-item rebuild cost, Selector narrows rebuild scope, and ValueKey ensures correct item identification — together reducing total per-frame workload. A, C, D don't match how these techniques actually work together.


Question 300. Per Module 12's summary, what is the ultimate goal of the entire process of optimizing and packaging the Taskly app? ⏎ A. Simply to reduce the project's number of lines of code ⏎ B. To produce a performance-optimized Release build, verify the app's size, and be ready for real-world deployment (production-ready) ⏎ C. To completely eliminate the need for writing Unit Tests in future projects ⏎ D. To convert the entire app from Flutter to a native platform
Answer: B. The ultimate goal is a performance-optimized, size-verified, production-ready Release build, per Module 12's Summary. A, C, D don't match the module's stated final goal.


Question 301. What is the basic difference between Implicit Animation and Explicit Animation in Flutter? ⏎ A. Implicit Animation needs a manual AnimationController, Explicit Animation doesn't ⏎ B. Implicit Animation automatically interpolates between old and new values when a property changes, without needing to manage an AnimationController manually; Explicit Animation requires explicitly declaring and controlling an AnimationController ⏎ C. The two animation types are entirely identical, just different names ⏎ D. Implicit Animation only works on iOS, Explicit Animation only works on Android
Answer: B. Implicit Animation auto-interpolates without a manual controller; Explicit Animation requires explicit AnimationController declaration/control. A, C, D don't match the actual technical characteristics.


Question 302. Which of the following widgets is a textbook example of Implicit Animation? ⏎ A. AnimationController ⏎ B. AnimatedContainer ⏎ C. Tween ⏎ D. CurvedAnimation
Answer: B. `AnimatedContainer` is a textbook Implicit Animation widget — it auto-animates when its properties (color, size...) change. A, C, D are components belonging to Explicit Animation or supporting tools, not an Implicit Animation widget itself.


Question 303. What required parameter does `AnimationController` need to work, illustrating the connection between animation and widget lifecycle? ⏎ A. `context` — because animation must be tied to BuildContext ⏎ B. `vsync` — usually provided by `TickerProviderStateMixin`, to sync the animation with displayed frames and avoid wasting resources when the widget isn't visible ⏎ C. `theme` — so the animation applies the correct color ⏎ D. `key` — so the animation can be tested
Answer: B. `vsync` (typically from TickerProviderStateMixin) syncs the animation with display frames, avoiding wasted resources when not visible. A, C, D are not AnimationController's required parameter.


Question 305. What is `CurvedAnimation` in Flutter used for? ⏎ A. Drawing a geometric curve shape on screen ⏎ B. Applying an easing curve (e.g. `Curves.easeIn`) onto an existing linear Animation, changing how the animation's value varies over time (uneven fast/slow) instead of linearly ⏎ C. Fully replacing AnimationController ⏎ D. Only usable with SliverAppBar
Answer: B. CurvedAnimation applies an easing curve onto a linear Animation, making its value change non-linearly over time. A, C, D misdescribe its function.


Question 306. Compare using `AnimatedContainer` (implicit) versus manually writing `AnimationController` + `Tween` + `AnimatedBuilder` (explicit) for a simple background color-change effect on button tap. When should the explicit approach be preferred despite being more complex? ⏎ A. Never use explicit, implicit is always better in every case ⏎ B. When you need detailed control over the animation (pausing, reversing, conditional repeating, synchronizing multiple animations at once, or reacting to complex events) — capabilities that a simple implicit widget doesn't provide ⏎ C. Explicit is always faster than implicit in render performance so it should always be used ⏎ D. Both approaches have an entirely identical API so there's no difference to consider
Answer: B. Explicit should be preferred when detailed control is needed (pause, reverse, conditional repeat, sync multiple animations, complex event reactions) — beyond simple implicit widgets. A, C, D don't match the actual selection principle.


Question 307. BLoC (Business Logic Component) is a state-management architecture pattern in Flutter, based on which core concept? ⏎ A. A parent widget passes data directly to a child widget via the constructor ⏎ B. The input stream is Events, the output stream is States — BLoC receives an Event, processes business logic, then emits a new State for the UI to listen to and update ⏎ C. Only using SharedPreferences to store state ⏎ D. There's no core concept, BLoC is just another name for setState
Answer: B. BLoC's core concept: input stream = Events, output stream = States, processing business logic in between. A, C, D misdescribe BLoC's fundamental nature.


Question 308. In the `flutter_bloc` package, what is the correct data-flow relationship between Event, Bloc, and State? ⏎ A. State → Bloc → Event ⏎ B. UI sends an Event into the Bloc → Bloc processes and maps the Event into a new State → Bloc emits the State → UI listens to the State to rebuild ⏎ C. Event and State are the same concept, interchangeable ⏎ D. Bloc only receives State, not Event
Answer: B. The correct flow: UI sends Event → Bloc processes/maps to new State → emits State → UI listens and rebuilds. A reverses this; C, D misdescribe the relationship between Event/State/Bloc.


Question 309. How does `Cubit` in the `flutter_bloc` package differ from a traditional `Bloc`? ⏎ A. Cubit cannot manage state, only Bloc can ⏎ B. Cubit is a simplified version of Bloc — it doesn't need a separate Event class defined, instead directly calling methods to emit new States, reducing boilerplate compared to a full Bloc ⏎ C. Cubit only works with int data ⏎ D. Cubit and Bloc are entirely identical in API, no difference
Answer: B. Cubit is a simplified Bloc — no separate Event class needed, methods directly emit new State, reducing boilerplate. A, C, D misdescribe Cubit's characteristics.


Question 311. Per BLoC pattern's design philosophy, why should business logic be entirely separated from the UI widget? ⏎ A. Because Flutter doesn't allow writing logic inside a widget ⏎ B. To increase testability (testing logic independently without building the UI), reuse logic across different platforms/screens, and keep the widget focused on display ⏎ C. Because BLoC runs faster if the logic is inside the widget ⏎ D. No clear reason, it's just a naming convention
Answer: B. Separation increases testability, allows logic reuse across platforms/screens, and keeps widgets focused on display. A, C, D misdescribe the purpose of this design philosophy.


Question 312. What is the basic difference between `SliverAppBar` and a regular `AppBar`? ⏎ A. SliverAppBar can only display icons, not text ⏎ B. SliverAppBar is designed to work inside a `CustomScrollView`, able to expand, shrink, or show/hide flexibly based on scroll behavior, while a regular AppBar has a fixed position ⏎ C. SliverAppBar cannot contain action buttons ⏎ D. The two widgets are entirely identical, just different names
Answer: B. SliverAppBar works inside CustomScrollView with flexible expand/shrink/show-hide on scroll, unlike a fixed-position regular AppBar. A, C, D misdescribe the characteristics.


Question 313. Which parent widget is `SliverAppBar` usually used with to function correctly? ⏎ A. `Scaffold` directly, with no other wrapping widget needed ⏎ B. `CustomScrollView`, placed inside the `slivers: [...]` property ⏎ C. A regular `ListView` (non-sliver form) ⏎ D. `Column`
Answer: B. SliverAppBar is typically used with CustomScrollView, placed inside `slivers: [...]`. A, C, D don't match standard usage.


Question 314. What are the `flexibleSpace` and `expandedHeight` properties of `SliverAppBar` used for? ⏎ A. `flexibleSpace` defines the content shown when the AppBar is in its expanded state (e.g. a large background image); `expandedHeight` sets the maximum height when the AppBar is fully expanded — as you scroll down, the AppBar gradually shrinks to its standard height ⏎ B. These two properties only affect color, not size ⏎ C. `expandedHeight` determines the maximum number of action buttons ⏎ D. `flexibleSpace` only works when there's no `CustomScrollView`
Answer: A. flexibleSpace defines the expanded-state content; expandedHeight sets the maximum expanded height, shrinking on scroll. B, C, D misdescribe these two properties' functions.


Question 315. In the Sliver ecosystem, how do `SliverList` and `SliverGrid` differ from a regular `ListView`/`GridView`? ⏎ A. No difference at all, the two types are entirely equivalent in every context ⏎ B. `SliverList`/`SliverGrid` are designed to combine multiple different scrollable content types (like SliverAppBar, SliverList, SliverGrid) within a SINGLE `CustomScrollView` with a seamless scroll effect, while `ListView`/`GridView` are independent scrollable widgets, hard to coordinate scroll effects with other scrollable widgets ⏎ C. SliverList only displays up to 10 elements ⏎ D. SliverGrid doesn't support displaying images
Answer: B. SliverList/SliverGrid are designed to combine multiple scrollable contents seamlessly in one CustomScrollView, unlike independent ListView/GridView. A, C, D don't match the actual technical characteristics.


Question 316. What does Golden Testing in Flutter check? ⏎ A. Asynchronous data-processing logic (Future/Stream) ⏎ B. Comparing a widget's rendered UI against a saved reference ("golden") image, to detect unintended visual regressions ⏎ C. Checking API response speed ⏎ D. Checking the app's security
Answer: B. Golden Testing compares rendered UI against a saved golden reference image to detect visual regressions. A, C, D misdescribe this test type's purpose.


Question 317. In `flutter_test`, which function is used to compare the current widget against a saved golden image? ⏎ A. `expect(find.text(...), findsOneWidget)` ⏎ B. `matchesGoldenFile('image_label.png')` combined with `expectLater()` ⏎ C. `tester.pumpAndSettle()` ⏎ D. `verify()`
Answer: B. `matchesGoldenFile()` combined with `expectLater()` is the standard flutter_test API for comparing against a golden image. A, C, D are not the correct function for this purpose.


Question 318. A student group runs a Golden Test on Windows, but the original golden image was created on macOS, causing the test to fail repeatedly even though the UI looks "identical" to the naked eye. What is the most common cause of this issue? ⏎ A. Golden Tests never work reliably on any operating system ⏎ B. Font rendering and anti-aliasing can differ slightly between operating systems/Flutter versions, causing pixel-by-pixel images to not match exactly even if hard to notice by eye — so Golden Tests are usually recommended to run consistently on the same environment (e.g. a fixed CI) to avoid false failures ⏎ C. Because SliverAppBar is incompatible with Golden Test ⏎ D. Because Windows doesn't support running `flutter test`
Answer: B. Slight cross-platform font-rendering/anti-aliasing differences cause pixel mismatches, hence the recommendation to run Golden Tests on a consistent environment (fixed CI). A, C, D don't match the real technical cause.


Question 319. In the general Software Development Life Cycle (SDLC), what is the main role of the "Analysis" phase? ⏎ A. Writing detailed source code for each feature ⏎ B. Gathering and clarifying requirements from users/clients before starting design and coding ⏎ C. Packaging and publishing the product to a store ⏎ D. Running the final automated test before release
Answer: B. The Analysis phase focuses on gathering/clarifying requirements before design and coding begin. A, C, D are other SDLC phases, not Analysis.


Question 320. What does "Technical Debt" refer to in software development? ⏎ A. A direct financial cost paid to a cloud service provider ⏎ B. The future cost (time to fix bugs, difficulty maintaining, difficulty scaling) arising from rushed or unoptimized design/coding decisions made now, similar to a "debt" that must be paid off gradually over time ⏎ C. The maximum number of lines of code allowed in a project ⏎ D. A mandatory fee paid when publishing an app to Google Play/App Store
Answer: B. Technical Debt is the future cost from rushed present-day design/coding decisions, like a debt paid off over time. A, C, D don't match the actual concept.


Question 321. A group of students intentionally skips writing Unit Tests and separating clear architecture (State/Repository) to "finish the project quickly," resulting in it later being very hard to fix bugs or add new features without breaking working parts. What concept from the lecture does this illustrate, and how does it relate to the architectural principles (Repository Pattern, Testing Pyramid) taught in Module 3 and Module 11? ⏎ A. This is unrelated to any concept taught in previous modules ⏎ B. This is an example of "Technical Debt" — skipping clean architecture (Repository Pattern from Module 3) and testing (Testing Pyramid from Module 11) saves short-term time but increases long-term maintenance/bug-fixing costs, matching the "borrow now, pay interest later" nature of technical debt ⏎ C. This is an example of Golden Testing ⏎ D. This is the recommended approach for performance optimization per Module 12
Answer: B. This is a Technical Debt example: skipping clean architecture and testing saves short-term time but raises long-term maintenance costs, matching debt's "borrow now, pay later" nature. A, C, D don't match the correct concept/relationship.


Question 322. In the software development lifecycle, where is the "Testing" phase usually placed relative to "Implementation" (coding)? ⏎ A. Testing always happens before Implementation in every development model ⏎ B. Testing usually happens after (or continuously in parallel with, depending on Agile/TDD models) the Implementation phase, to verify the written code works as required per the analysis ⏎ C. Testing has no relationship with Implementation at all ⏎ D. Testing is only ever done once, after the app has been released
Answer: B. Testing usually happens after (or in continuous parallel with, per Agile/TDD) Implementation, verifying the code meets requirements. A, C, D don't match Testing's usual position in the SDLC.


Question 323. Consider the Taskly app (from Modules 11/12) rewritten with BLoC architecture instead of Provider: `TaskBloc` would receive `AddTaskEvent` and `ToggleTaskEvent`, then emit a new `TaskState`. Compared to using `TaskProvider` (ChangeNotifier) as in the original slides, what is the core difference in the processing flow? ⏎ A. No difference at all, BLoC and Provider are entirely identical in how they receive changes from the UI ⏎ B. With ChangeNotifier (Provider), the UI directly calls a method on the Provider (e.g. `taskProvider.addTask(...)`) and the Provider mutates itself then calls `notifyListeners()`; with BLoC, the UI must "dispatch" an Event object (e.g. `bloc.add(AddTaskEvent(...))`) into the input stream, and the Bloc processes that Event to emit a new State, entirely decoupled from a direct call ⏎ C. BLoC cannot be used to build a Todo app like Taskly ⏎ D. Provider is required to use Events just
Answer: B. Provider uses direct method calls + notifyListeners(); BLoC uses dispatched Events processed into emitted States, decoupled from direct calls — this is the core flow difference. A, C, D don't match how the two architectures actually operate.


Question 324. Why do many real-world Flutter projects choose BLoC instead of just `setState` for large-scale apps with many screens sharing complex state? ⏎ A. Because `setState` isn't supported by Flutter in large apps ⏎ B. BLoC clearly separates the Event/State flow, making logic easier to test independently (without building UI), easier to trace data flow explicitly, and scales better when many screens need to share and react to the same complex state source — something local `setState` struggles with as an app grows ⏎ C. `setState` only works with exactly 1 screen in the entire app ⏎ D. BLoC is always faster than setState in absolute render performance in every case
Answer: B. BLoC's clear Event/State separation aids independent testability, explicit data-flow tracing, and better scaling for shared complex state across many screens. A, C, D don't match the real reasoning for choosing BLoC.


Question 325. In `flutter_bloc`, how does the `BlocListener` widget differ from `BlocBuilder`?
⏎ A. The two widgets are entirely identical, interchangeable in every case
⏎ B. `BlocBuilder` is used to rebuild the UI based on the new state (returns a widget); `BlocListener` is used to perform one-time side-effects (e.g. navigation, showing a SnackBar/Dialog) when the state changes, WITHOUT rebuilding the UI
⏎ C. `BlocListener` can only be used with Cubit, not with Bloc
⏎ D. `BlocBuilder` is only used for Golden Testing
Answer: B. BlocBuilder rebuilds UI based on new state; BlocListener performs one-time side-effects on state change without rebuilding. A, C, D misdescribe the difference between these two widgets.


Question 326. If a Golden Test is set up to run automatically in a CI/CD pipeline before merging code, what is the main purpose of doing this?
⏎ A. Increases the speed of building the release app
⏎ B. Automatically catches unintended visual regressions caused by code changes early, before those changes get merged into the main branch, helping ensure UI consistency over time
⏎ C. Fully replaces Unit Test and Widget Test
⏎ D. Only has the effect of checking the app's network speed
Answer: B. Running Golden Tests in CI/CD catches visual regressions early before merging, ensuring UI consistency over time. A, C, D don't match the actual purpose of this CI/CD integration.
